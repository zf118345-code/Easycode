# EasyCode 协作约束

## 权威文档

开始产品、架构、交互或代码工作前，按顺序阅读：

1. `PRODUCT.md`：产品目标、范围、质量预算与非目标。
2. `docs/ARCHITECTURE.md`：领域模型、边界、数据契约和平台架构。
3. `design.md`：信息架构、交互与 Control 组件规范。
4. `docs/TEST_HARNESS.md`：验收、证据和资源保护规则。
5. `docs/DECISIONS.md`：已冻结的关键决策。
6. 任务涉及的 `docs/vnext/*.md` 细分规范。

若实现与文档冲突，先说明冲突并更新规格，不能静默选择任一版本。

## 开工门

- 中大型需求先完成代码库事实审计和未知项盘点。
- 规格讨论按“产品与领域框架 → 编译/运行/扩展边界 → 领域能力 → 工作区交互 → Control、命名与视觉细节”推进；上层未冻结时，不用局部 UI 或字段设计反向决定架构。
- 需求必须有稳定编号、完成状态、范围外内容、失败语义和可验证验收标准。
- 会改变领域模型、持久化、跨平台语义或 Player 契约的决定必须写入 `docs/DECISIONS.md`。
- 规格仍有会改变实现路线的开放问题时，不进入实现。
- 旧项目无需兼容；不得为了废弃测试项目引入迁移层。

## 实施规则

- 使用垂直切片：Schema、后端、前端、持久化、执行、Player、错误恢复、测试和文档一次闭环。
- 不保留只有外观没有后端承接的入口，也不宣称未经过验证的能力。
- 版本化强类型 `ProgramDocument` 是项目函数唯一可编辑事实；纵向结构化语句是派生投影，ECIR 是确定性构建产物。不得把可编辑源码、Monaco、解析草稿或文本反向解析重新引入为平权数据源。
- 功能扩展的版本化命名空间模型是该扩展唯一业务事实；扩展贡献的列表、图形或其他视图只能作为派生或受约束编辑投影。页面导航不属于首个正式版本，未来需要时必须重新立项，不得依据延期记录实现。
- IDE 与 Player 共用强类型参数契约和 Control 原子组件，不强行共用整页布局。
- 默认串行运行重型测试和构建；先检查是否已有重复 Vite、pytest、Python 或打包进程。
- 不关闭 Codex、贝贝 VPN、ToDesk/TD 远程连接，也不批量结束未知系统进程。

## 完成门

- 每条需求都能追踪到验收项、测试和证据。
- 自动化通过不等于真实宿主通过；Windows 窗口、模拟器、捕获、独立 Windows Player 和真实 Android 设备必须在对应环境冒烟。仅通过 `adb screencap/input` 操作设备不算 Android 本机运行时证据。
- 首个正式版本不得在 Android 本机垂直切片完成前放行：签名 APK 必须能在断开数据线并关闭 IDE、后端后独立启动，完成权限、捕获、输入、文件、Player 表单、扩展闭包和结构化日志验收。
- 未声明公网能力的发布项目必须在阻断公网、清空预热缓存并关闭 IDE/开发后端后通过 Windows 与 Android 离线冒烟；发布期间未发现的运行期下载、在线验权、更新、遥测或扩展外连均视为完成门失败。
- 在线更新必须分别验证关闭时零外连、签名/防回滚、任务版本固定、Windows 原子恢复和 Android 内容/APK 两条真机路径；更新服务故障不得冒充任务运行故障。
- Player 字段捕获/文件动作必须从作者逐字段配置、发布权限闭包、终端真实按钮、系统授权、原子回填到失败恢复闭环；仅预览可见或仅 IDE 可用不能算完成。
- 本地计划与 LAN 批量运行必须在真实 Windows Hub 和 Android 真机验证；同一派发重试只能创建一次运行，普通消息不能获得远程启动权限。
- 类型化 HTTP、实例配对、消息和调度必须分别验证权限与失败语义；平台不得自动上传运行/崩溃遥测，也不得展示没有数据承接的云端设备舰队。
- 报告必须区分：自动验证通过、人工验证通过、未验证、受环境阻塞。
- 删除旧实现或文档前检查引用；删除后更新权威文档索引。
