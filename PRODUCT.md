# EasyCode 产品规格

状态：`Approved`（首个正式版本产品边界、程序模型、官方函数目录与扩展交付契约已冻结）

最近修订：2026-09-01

> 本文件描述最终要实现的产品，而不是当前代码现状。实现状态与真实宿主证据单独进入 Harness；`Approved` 不表示当前代码已经完成。

## 1. 产品定义

EasyCode 是面向 Windows 软件、安卓模拟器和 Android 本机的结构化自动化开发环境。开发 IDE 运行在 Windows，并能测试 Windows、ADB 模拟器/设备和通过数据线连接的 Android 本机运行时；脚本制作者通过函数库、纵向结构化语句、类型化表单、资源与捕获工具组合程序，再发布为最终用户无需安装 Python 的独立 Player。首个正式版本必须同时交付 Windows IDE、Windows/ADB Player 和能够脱离电脑独立运行的 Android 本机 Player；iOS 与 macOS 不在产品范围内。

EasyCode 的核心价值不是“录制坐标”，也不是要求用户学习一门源码语言，而是把视觉、输入、控件、控制流、变量和领域扩展组织成可理解、可检查、可复用、可调试、可交付的自动化程序。

## 2. 目标用户

1. 不熟悉编程，但能理解条件、变量和执行顺序的脚本制作者。
2. 熟悉 Python 或原生开发，希望把复杂能力封装给普通用户使用的高级开发者。
3. 只在 Player 中选择运行方案、填写表单和执行任务的最终用户。

## 3. 核心用户结果

- 从左侧函数库、行内键盘意图或命令搜索选择能力，在中央按顺序组织语句，在右侧使用表单配置全部参数和返回绑定，无需维护源码。
- 取点、框选、录图、取色、控件捕获和路径捕获直接回填当前参数。
- 普通流程保持紧凑、可搜索、可重排、可折叠、可提取为项目函数，并能逐句调试。
- “调用并判断”“执行直到”等高频意图可以由一次作者操作生成普通强类型结构；便捷入口不得产生隐藏调用或第二套运行语义。
- 非通用领域能力通过可选功能扩展进入；普通项目不承担无关入口、后台任务和发布体积。
- 高级开发者在外部 IDE 编写 Python/原生扩展，EasyCode 负责契约、验证、引入、调用和发布闭包。
- 开发者使用与 IDE 同一套强类型参数契约和 Control 设计 Player 表单。
- Player 本地优先且独立运行：未显式使用公网能力的项目不依赖公网、IDE、EasyCode 云或单独安装的开发后端；错误可诊断、失败不伪报，发布内容不包含项目的可编辑程序文档或扩展源码。

## 4. 产品模型

```text
项目
├─ ProgramDocument：项目函数的唯一可编辑程序事实
├─ 函数注册表：官方函数、项目函数和扩展函数
├─ 项目变量与局部符号
├─ 图片、OCR、控件等资源
├─ Windows / Android ADB / Android 本机目标
├─ 可选功能扩展及其命名空间数据
├─ Player 表单、运行方案与稳定绑定
└─ ECIR、依赖闭包、测试配置和发布设置
```

### 4.1 程序模型

- 项目函数以版本化、确定性的 `ProgramDocument` JSON 持久化。
- 中央编辑区是 ProgramDocument 的纵向结构化语句投影，不是自由画布，也不是可手写文本编辑器。
- 左侧创建函数调用；中央处理执行顺序、嵌套、多选、重排、断点和运行状态；右侧修改当前语句的完整参数、返回绑定和结构属性。
- ProgramDocument 使用稳定 `function_id`、`statement_id`、`symbol_id`、`value_id` 和类型化值节点。中文名称和语句摘要只负责显示，不作为执行或长期绑定键。
- ProgramDocument 编译为版本化 ECIR。IDE 调试和 Player 执行同一 ECIR 语义，避免形成两套运行逻辑。
- 经核心批准的无副作用值操作允许完整递归组合；复杂度由摘要、折叠、聚焦和提取管理，不以浅层限制制造未来能力瓶颈。普通函数和 I/O 仍作为独立语句显示。
- 结构化控制流同时提供“跳出循环”“跳过本轮”和“结束为失败”。前两者只在当前项目函数的词法循环体内合法，并只作用于最近一层循环；“结束为失败”产生可被异常区域捕获的永久结构化错误，不伪装成普通函数或返回值。
- 结构化异常区域可以选择开启重试策略，但不增加独立“重试”语句或函数。策略默认关闭；开启后的默认值为首次失败后最多再试 2 次、固定间隔 500ms，并且只处理函数契约标记的瞬时结构化异常。

完整约束见 [结构化程序模型](docs/vnext/PROGRAM_MODEL.md) 与 [结构化值表达式](docs/vnext/EXPRESSIONS.md)。

### 4.2 函数层级

- **官方函数**：用户按“图像、文字、输入、等待”等用途查找；实现可以是原子操作，也可以是经过验证的标准组合，普通用户无需区分。
- **项目函数**：当前项目使用 ProgramDocument 组合出的可复用业务流程。
- **扩展函数**：项目级或用户级 Python/原生实现，经强类型契约注册为普通函数。

内部实现边界如下：

- 官方原子函数与扩展函数可以由 Python、C++、Kotlin 或平台驱动实现。
- 官方标准函数与项目函数使用 ProgramDocument 组合，保持可查看、可调试和跨平台编译。
- 性能敏感的官方标准函数可以拥有通过等价性测试的融合实现，但不得改变其参数、返回、取消、错误和日志语义。
- “精简核心”约束运行原语和用户概念，不要求普通用户只使用少量低级函数。能够由现有原语组合、在多个项目中反复出现且显著减少样板流程的能力，可以进入经过筛选的官方标准函数库；项目或行业特有组合继续使用项目函数或扩展。标准函数不得借便利之名增加第二套运行语义。

官方函数采用 `命名空间.动作` 的规范显示形式；自然语言、旧名称和同义词只作为搜索别名，不进入 ProgramDocument、Player 绑定或发布契约。每个项目锁定所用函数的稳定 `function_id`、契约版本与契约指纹；IDE 可以提示兼容更新或迁移，但不得在打开项目时静默改写。破坏性契约变化产生新的主版本，旧项目在弃用期后仍能使用已锁定兼容实现构建。默认值只用于跨项目、跨平台仍然安全且含义稳定的参数；图片、路径、目标、窗口选择器等不能可靠猜测的必填值保持 `unset`。每个函数分别声明 Windows、ADB、Android 本机与无目标场景的支持状态，不使用笼统的“支持 Android”。

对可重复执行的外部任务，推荐项目函数采用“执行前观察—必要时执行—执行后验证”的写法。它是脚本设计模式，不新增 `任务.确保完成` 特殊函数、隐藏缓存或持久任务状态。

### 4.3 变量模型

用户只需要理解两类变量：

- **局部变量**：函数参数、赋值、循环项、异常和函数结果产生的符号，只在当前函数调用内有效。
- **项目变量**：项目显式定义，所有项目函数共享当前运行副本；任务启动时从项目默认值和当前 Player 运行方案创建，任务结束后丢弃且不回写。

项目变量不再区分“配置变量、本次运行变量、持久变量”。需要与本机文件交换长期数据时显式调用 `文件.*`；多实例与局域网传递使用 `消息.*`。当前时间、项目目录和目标尺寸等环境信息由有类型的函数获取，不建立系统变量表。

### 4.4 文件与消息

- `文件.*` 首版提供读取、写入、追加、替换、JSON、复制、移动和删除等明确文件操作；`目录.*` 首版提供存在、创建、列出、复制、移动、删除空目录与受控递归删除，不另建“持久变量”概念。
- 递归删除只能使用具有独立危险权限的 `DirectoryReference`，并受用户明确选择的授权根约束。作者审核、IDE 调试或 Player 对真实授权根的执行配置确认、根目录保护、链接越界防护和部分完成报告共同构成首版完成门；普通目录选择不自动授予递归删除。目录复制/移动的冲突策略不允许合并或覆盖既有目录，不能借此绕过危险确认。
- `消息.*` 面向同机或局域网内的 Player 实例传递结构化值。发送方可以指定一个或多个明确收件实例；每个收件人拥有独立投递、已读、过期和取消状态。同一操作系统用户下、同一签名产品的本机实例可以自动建立信任；跨电脑实例必须通过短期配对码或二维码确认，显示名称不作为安全身份。
- 已读只表示接收脚本已经取得并解码数据，不表示后续游戏操作成功。
- 接收方可以同步等待，也可以声明监听器。监听器在安全检查点串行处理，不创建第二条并发界面操作流；处理函数正常结束后，从被挂起主流程的下一条语句继续。
- 主动暂停只在同一 Player 进程内保留现场；停止、取消、进程退出或崩溃后不恢复旧调用栈。诊断日志保留异常位置和证据，但下一次运行使用新运行 ID 从入口开始。

### 4.5 目标平台

- **Windows 窗口/桌面**：优先后台截图、控件和后台输入；不支持时按项目策略回退物理输入，并记录真实路径。
- **Android ADB**：使用设备帧、点击、滑动和文本输入，不占用 PC 物理鼠标。
- **Android 本机**：属于首个正式版本的交付范围。开发端通过 USB/ADB 部署、调试和采集证据，手机端以 Android 本机宿主执行同一 ECIR；签名 APK 必须在断开数据线并关闭 IDE、后端后独立运行。真实宿主、权限、捕获、输入、文件和打包闭环完成前不显示可用入口，也不得宣称首个正式版本完成。
- **无操作目标**：允许纯计算、文件、消息和日志任务，但视觉与输入函数不可用。

Windows Player 可以在同一串行流程中显式切换 Windows 桌面/窗口与 ADB 目标；任意时刻只有一个当前目标。Android 本机实例只操作当前手机。需要真正同时操作多个目标时，使用多个隔离 Player 实例并通过消息协作。

Windows 宿主首版提供直接启动应用、等待该应用退出，以及查找/等待、激活、关闭和读取 Windows 窗口状态的官方能力。它们不得退化为任意 Shell、PowerShell、命令解释器或系统命令入口；特殊系统自动化继续由经过权限审核的扩展提供。启动模拟器后操作 ADB 使用“启动应用 → 等待目标上线 → 进入目标作用域”的普通组合，不增加另一套模拟器流程语义。

普通图像与 OCR 调用在执行时自动取得当前目标的新帧；需要同一时刻比较多个特征时，开发者可以显式取得一个不可变帧并供多个视觉调用复用。帧与由其产生的坐标、区域和结果携带来源目标及空间版本，不能跨目标静默使用。立即查找未命中，以及等待图片、文字或控件到期，都是可由条件处理的正常“无结果”；资源缺失、权限不足、目标断开和驱动/扩展崩溃才产生结构化异常。

Android 本机的首版交付闭环见 [Android 本机 Player](docs/vnext/ANDROID.md)。

### 4.6 扩展模型

扩展分为函数扩展、功能扩展和受信任目标驱动：

- 函数扩展注册类型化函数及实现。
- 功能扩展可以贡献完整工作区、领域模型、编译步骤、运行模块和 Player 模块。
- 目标驱动接入截图、输入、设备和原生权限，仅允许官方或明确受信任实现。

首个正式版本只交付通用扩展地基，不交付页面导航扩展、Page Model、`页面.*` 函数、页面扫描或拓扑工作区。未来若重新立项页面导航，必须重新冻结独立功能规格、跨平台语义与 Harness，不能把历史草案视为授权实现。首版不建设公开市场、评分、付费或远程代码安装。

扩展唯一清单固定为 `easycode-extension.json`，项目解析结果固定写入 `easycode.lock`。清单分别声明宿主和可操作目标：Windows 宿主变体可以服务 Windows 与 ADB 目标，Android 本机宿主只能选择 Android 本机变体；“支持 Android”不能替代这两个维度。项目启用状态、安装范围和本机信任决定属于项目/IDE 本机事实，扩展不能在自己的清单中自行授予。

Python 或其他可执行扩展属于开发者明确批准的可信本地代码。Worker 负责超时、取消、日志、序列化和崩溃隔离，不承诺抵御恶意代码访问同一操作系统账号资源；权限声明用于宿主 API 执行、审计和发布闭包，不能被宣传为恶意代码沙箱。未经本机信任的第三方包只能使用不执行扩展代码的声明式安全贡献子集。

IDE 可以在开发调试中运行明确受信任的扩展源码，但 Windows Bundle、`.ecplayer` 与 Android APK 只允许携带构建、哈希、签名和锁定后的密封运行产物。扩展源码、测试、外部 IDE 配置和开发入口均被 Bundle Loader 拒绝。密封产物用于不可变交付和源码隔离，不等同于 DRM，也不改变代码执行所需的本机信任边界。

完整约束见 [扩展平台](docs/vnext/EXTENSIONS.md)。

### 4.7 本地优先与网络边界

- 发布器从真实函数、扩展和服务依赖自动判定“无网络 / 本机或局域网 / 公网”，并在发布报告中定位来源。
- 无公网项目把 Runtime、函数、资源、OCR 数据、Control、原生库和扩展完整打包；启动与执行不隐式登录、在线验权、下载依赖、检查更新或上传遥测。
- 同机/LAN 消息是显式局域网能力，不依赖 EasyCode 云；显式公网函数失败只影响当前调用及其普通错误处理。
- 首版把普通文本/JSON HTTP 请求、multipart 文件上传和文件下载作为三项独立的类型化原子能力。上传流式读取已授权 `FileReference`；下载先流式写入受控临时文件，完整成功后再原子提交到目标。文件字节不进入 ProgramDocument、项目变量或普通日志，也不把文本、JSON、文件和任意二进制流塞进一个万能请求函数。浏览器 DOM 自动化、网页会话接管和验证码处理不进入核心，未来通过扩展按真实需求建设。
- 首版提供可关闭的在线更新：EasyCode IDE、Player 应用和项目内容分别更新；普通作者可使用 EasyCode 官方托管，高级作者可使用同一开放协议接入自建 HTTPS 对象存储/CDN。更新属于维护网络，不改变任务运行网络结论。服务失败时当前运行/暂停现场和没有已生效最低版本约束的本地任务继续可用；已经缓存且超过宽限期的可信强制策略仍阻止旧版本新开任务。项目关闭更新后不配置端点、不发起更新请求。商业授权和其他公网服务没有独立规格与真实闭环前不显示入口，也不能成为 Player 启动或任务执行的隐藏前置条件；平台自动遥测明确不属于首版。

完整约束见 [本地优先与离线运行](docs/vnext/OFFLINE.md) 与 [网络、HTTP 与设备配对](docs/vnext/NETWORK.md)。

### 4.8 计划与批量运行

- 计划属于 Player 宿主控制面，不属于 ProgramDocument，也不增加 `计划.*` 脚本函数。首版支持单次、每天和固定间隔三种本地计划。
- Windows 的 Player Hub 可以按一个批次启动多个已安装项目与运行方案，也可以把启动命令派发给已配对且明确授予远程启动权限的局域网实例。同一实例严格串行，不同实例可以并行或按确定性间隔错峰。
- Android 本机使用系统调度启动当前 APK 内的运行方案；首版不让一个项目 APK 枚举或启动其他独立项目 APK。Android 后台限制、强制停止和省电策略造成的延迟必须如实显示，不能承诺桌面级精确到秒。
- 局域网远程启动是带 `dispatch_id` 的幂等控制命令，不等同于普通 `消息.*`。配对只建立身份信任，远程启动权限单独授予并可撤销；协调端离线时其他实例不自行猜测计划。

完整约束见 [计划与批量运行](docs/vnext/SCHEDULES.md)。

### 4.9 运行录制与离线回放

- 完整目标帧录制与离线分析属于首个正式版本，不是后续插件。Windows、ADB 与 Android 本机使用同一版本化录制会话、目标片段、帧和分析报告契约。
- Recorder/Replay 属于 IDE 与 Player 宿主能力，不增加 `录制.*` 或 `回放.*` 脚本函数。Windows Bundle 与 Android APK 必须携带兼容模块；默认关闭表示零后台活动，不表示发布器可以删掉这项首版能力。
- 录制支持全部帧、变化帧和诊断三种策略，默认关闭。Player 只能由终端用户主动开启或确认到当前运行方案 revision；更新不能静默开启。写盘采用有界缓冲、磁盘保护和明确丢帧事件，停止录制不停止正在运行的任务。
- 离线回放支持时间线、帧比较、官方图像/OCR 分析及从历史帧取点、框选和录入资源。像素帧不承诺重建 UIA/Accessibility 控件树；第三方扩展回放在具备独立能力隔离前不进入首版。回放不重新执行 ECIR、输入、文件、消息、网络或其他外部副作用，也不恢复崩溃任务。
- 录制默认只保存在当前设备，不进入项目、Git、`.ecplayer`、APK 或在线更新，也不自动上传；只有用户显式导出后才能离开设备。

完整约束见 [帧录制与离线回放](docs/vnext/REPLAY.md)。

## 5. 首个正式版本必须形成闭环的能力

### 5.1 开发

- 项目打开、新建、结构化自动保存、外部冲突比较与恢复。
- ProgramDocument 的创建、结构化编辑、Schema/引用校验、搜索、跳转、撤销、重做和提取函数。
- 函数浏览、显式插入、项目函数编辑，以及扩展骨架、导入、契约验证和外部 IDE 打开。
- 所有函数形参、返回绑定、条件、循环、循环控制、显式失败和监听结构的类型化检查器。
- 完整递归的纯值计算、稳定嵌套值绑定，以及计算摘要、折叠、聚焦与结构化提取。
- 完整的记录、列表、字典与外部 JSON 数据模型；支持结构化筛选、转换、排序、分组、统计、循环以及 IDE/Player 友好表单。
- 局部变量与项目变量的声明、类型、引用、稳定重命名和运行时监控。
- 资源录入、选择、预览、移动、替换、删除、引用修复和统一 Capture Session。
- 断点、单步、变量、问题、分类日志、失败证据，以及录制会话、历史帧时间线、比较、离线分析和历史帧 Capture 工作区。
- 文件与目录函数的强类型检查器、权限闭包和跨平台预览；递归删除必须以独立危险动作显示真实目标、不可回滚语义与作者审核状态。
- 真实扩展发现、启用、禁用、锁定、发布和失败诊断；未实现能力不展示入口。
- 类型化文本/JSON HTTP 请求、独立文件上传与原子文件下载、真实错误语义和发布网络/文件权限报告形成闭环；网页 DOM 自动化和万能二进制流不作为其隐藏子能力。
- Windows 应用启动/退出等待、窗口查找/等待/激活/关闭/状态读取形成闭环，并证明官方函数不能执行任意 Shell 或系统命令。
- 视觉函数覆盖自动新帧、显式同帧复用、来源目标隔离，以及正常无结果与结构化异常的完整分流。
- 异常区域重试覆盖默认关闭、瞬时/永久异常分类、正常无结果、取消、暂停、次数耗尽、副作用确认、无回滚和 IDE/Player 等价执行。

### 5.2 运行与交付

- Windows、ADB 与 Android 本机运行时按同一 ECIR、函数契约和 Player Schema 语义运行；Windows Player 支持 Windows/ADB 串行目标切换，Android 本机 Player 只操作当前手机。
- Windows IDE 可以向 USB 连接的真实 Android 设备部署或更新测试 Player，读取结构化日志与诊断，并明确区分“以 ADB 作为操作目标测试”和“Android 本机运行时测试”。
- Android 本机 Player 完成截图、图像/OCR、点击、滑动、拖拽、长按、文本输入、Capture Session、权限生命周期、应用私有/用户授权文件和前后台生命周期闭环。
- Windows、ADB 与 Android 本机完成全部帧/变化帧/诊断录制、磁盘与背压保护、本机浏览、无副作用离线分析、显式导出和隐私闭环；任一平台未通过真实宿主验证时首版不得放行。
- Windows 与 Android 本机完成目录存在、创建、列出、复制、移动、删除空目录和受控递归删除；范围无法验证的平台位置必须在执行前拒绝。
- 消息支持一个或多个明确实例、同步等待、条件监听、已读与过期语义。
- Player 表单支持全部已批准的强类型 Control、校验、条件显示和稳定绑定。
- 开发者可以逐字段、逐动作、逐平台决定是否向 Player 开放取点、框选、当前画面截取、目标画面取色、设备选图、控件捕获、手势路径录制和文件/目录选择，默认全部关闭。开放后 Player 必须渲染真实 Control 动作并在用户触发时按需请求最小权限；不支持的动作不能显示假按钮或降级为普通字符串。
- Player 支持本地单次、每日和固定间隔计划；Windows Hub 支持定时批量启动多个项目/运行方案并向已配对局域网实例派发，Android 只调度当前 APK 内方案。
- 发布前检查目标平台、资源、扩展、依赖、权限、未绑定参数和不支持能力。
- 未声明公网能力的 Windows Bundle 与 Android APK 在阻断公网、关闭 IDE 与开发后端后仍能完成本地核心流程；发布报告列出真实网络级别和全部来源。
- `.ecplayer` 与独立 Player 运行时不包含 ProgramDocument、可编辑项目数据或扩展源码。
- Android 发布生成可安装的签名 APK；APK 内包含 Android 运行时、Player UI、兼容 ECIR、资源与已构建扩展闭包，不依赖 Windows、IDE、项目目录或 Python。AAB 只在未来明确需要应用商店分发时另行纳入发布门。
- IDE 与已发布 Player 支持服务器更新。日常脚本变更优先发布签名项目内容包；Runtime、宿主、原生扩展、权限或 ABI 变化发布新的 Windows Bundle/Android APK。更新检查、下载或安装失败不能中断当前运行/暂停现场；没有已生效最低版本约束时也不能阻止当前有效版本新开本地任务。
- 官方托管提供作者账号、项目归属、上传、不可变产物存储/CDN、通道、基础配额、使用量、审计和监控；自建源使用相同签名 Feed。发布私钥不进入服务器，终端 Player 获取公开更新不依赖作者账号。
- 作者可以发布可选更新或带生效时间、宽限期和原因的签名强制更新。强制更新从不打断运行/暂停现场，只在宽限结束后阻止不兼容旧版本开始新任务；Player 外壳、日志、诊断和更新恢复保持可用。
- 作者启用更新后，终端用户可以分别关闭普通可选更新的自动检查、自动下载和自动应用，并始终保留手动检查。关闭普通自动检查不会关闭已明确披露的轻量签名强制策略检查；一旦 Player 取得并验证强制策略，终端只能在宽限期内选择更新时间，不能删除或绕过最低版本约束。作者在打包时关闭更新的 Player 不显示更新入口，也不携带更新端点或任何更新调度器。
- 可选更新支持测试通道、稳定白名单、确定性百分比灰度、暂停/继续和全量推广。分组基于本机随机且项目隔离的匿名代码，由客户端本地计算，不上传硬件身份或运行遥测；比例扩大不重新洗牌。强制更新不参与白名单或百分比分割，只有目标发布对稳定通道 `100%` 可取得后才能启用。
- 测试通道由作者控制。稳定版终端不能自行切换进入测试通道；用户只把项目级匿名“测试分组码”交给作者，由作者加入或移出测试白名单。离开测试通道不自动降级，等待稳定通道追上当前发布序号。
- EasyCode 不自动上传运行结果、崩溃、日志、截图或在线状态。诊断默认保存在本机，只在用户明确导出后离开设备；官方更新托管只展示真实的请求量、传输量、配额与发布审计，不伪造安装成功率、业务成功率或作者设备舰队面板。
- 错误、取消、超时、输入回退和资源替换都有真实状态与结构化日志。ProgramDocument 只通过异常区域的可选策略重试瞬时结构化异常；消息、更新和派发等服务的内部幂等重试继续按各自契约记录，不能伪装成用户程序的异常区域重试。

## 6. 非目标

- 不兼容或迁移旧画布测试项目。
- 不让项目函数直接执行任意 Python；复杂实现通过扩展函数契约进入。
- 不把应用启动能力扩张为任意 Shell、PowerShell、脚本解释器或通用系统命令执行器。
- 不提供项目函数的可编辑源码视图，不把自由连线画布恢复为业务流程主编辑器。
- 不建设通用投影式语言平台，也不让普通扩展任意增加 ProgramDocument 语句种类。
- 首个正式版本不提供页面导航、页面拓扑、随机弹窗后台扫描或 `页面.*` 函数；普通脚本仍可用图像/OCR、条件、循环和项目函数自行表达相关业务。
- 首个正式版本不把浏览器 DOM 自动化塞进 HTTP/API，也不提供自动遥测、云端在线设备列表或远程设备管理面板。
- 不为尚未实现的 Android 本机、云协作或公开扩展市场保留假入口；签名 Player 更新是明确交付能力，不等同于允许市场式安装任意远程扩展代码。
- 首个正式版本不提供脚本收费激活、设备授权或在线授权管理；包签名、哈希和内容完整性校验仍属于首版发布闭环。
- 不支持 iOS 或 macOS 开发、运行与打包。
- 不追求规避第三方软件规则、验证码、安全机制或服务条款。

### 6.1 已确认的后续版本方向

作者效率增强已确认为首个正式版本完成后的下一实施序列：一次捕获生成确定性主/后备策略、把基础输入与目标事件录制为可编辑 ProgramDocument 骨架、以项目级触发器启动既有入口，以及通过第一方签名扩展包提供浏览器 DOM 自动化。它们必须遵循 [作者效率自动化能力](docs/vnext/AUTHORING_AUTOMATION.md)，不得修改 ProgramDocument Schema、引入第二套脚本或把浏览器行为塞入 HTTP 原子函数。

该序列先完成统一 UI 原语和真实扩展包导入路径。浏览器、Excel/CSV 等领域能力在开发与验收阶段先作为普通签名包由作者手动导入；验证稳定后只能把同一产物提升为官方随附包，不能维护硬编码内置版本。

商业授权是重要的后续独立垂直切片，不从长期路线图删除。未来版本需要同时完成作者侧授权策略、终端用户激活、离线可验证凭据、设备绑定与换机、续期/宽限期、吊销、授权服务、管理端、审计、安全和售后恢复流程后才显示入口。它必须继续遵守本地优先边界：不能要求每次启动或每个任务联网；有效本地凭据在断网时可验证；任务运行中到期不强制中断。具体商业模型与服务端技术选型留到该专题开始时重新调研，不为首版预建半成品 UI 或服务。

作者侧更新托管的 VIP、付费额度、私有分发、团队席位和加速线路同样保留为后续商业方向，但它与终端脚本授权相互独立。首版只建设可靠托管所需的作者身份、基础配额和使用量计量，不显示没有计费、退款和服务保障闭环的 VIP 入口。

页面导航、可视拓扑编辑、页面路径规划与随机弹窗统一处理保留为未来可能的独立功能扩展方向；只有出现明确项目需求时才重新立项，不为其预建首版模型、函数、工作区或后台服务。

## 7. 正确性与信任底线

- 用户编辑不能因切换工作区、保存冲突、校验失败或扩展故障而丢失。
- 前端每个可操作入口必须有后端、持久化、失败语义和测试承接。
- 后台输入不支持时不能伪报成功；物理回退必须记录原因和实际输入路径。
- 功能不可用时显示原因和恢复方式，不使用无反应按钮。
- 资源、变量、语句、函数、Player 绑定和扩展引用使用稳定 ID。
- 运行、发布和捕获严格绑定当前项目、当前目标和会话身份。

## 8. 性能目标

- 普通结构化编辑一帧内反馈；局部 Schema 校验 `<50ms`，跨函数语义校验 `<200ms`，函数检索 `<100ms`。
- 10,000 条结构化语句 `<1s` 可交互；大型列表使用虚拟化，折叠分支不创建不可见 Control。
- 1,000 个函数检索 `<100ms`；10,000 个资源使用虚拟化和缩略图懒加载。
- IDE 空闲时不持续截图、轮询或修正窗口；Player Hub 在没有到期计划、派发或实例状态变化时不得忙轮询，CPU 接近 0。
- 热视觉/输入执行路径不经过 WebView 或 HTTP 往返。
- 支持 1/2/4/8 实例隔离测试；24 小时运行无持续内存增长。
- Player SSD 冷启动目标约 3 秒。

## 9. 成功指标

- 新用户能在 15 分钟内完成“识图 → 点击 → 等待 → 输出日志 → Player 参数化”的可运行程序。
- 核心用户路径不存在只有前端或只有后端的半成品入口。
- P0 数据丢失、伪成功和跨项目污染问题为 0。
- 每个发布能力可追踪到需求、验收测试和环境证据。
- 中大型功能的返工原因可在 ADR、规格或未知项记录中定位，而不是只存在于聊天记录。

## 10. 规格讨论与实施顺序

后续讨论固定遵循“先大后小”：

1. 产品边界、核心领域对象和唯一事实来源。
2. 编译/运行模型、函数与扩展边界、跨平台边界。
3. 变量、表达式、控制流、资源、网络和计划等领域语义。
4. IDE/Player 工作区与跨工作区交互。
5. Control、字段、默认值、文案、间距和视觉细节。
6. 垂直切片实施、Harness 证据与真实宿主验收。

没有关闭会改变数据模型或执行路线的未知项，不进入对应实现。任何新功能必须使用 [功能规格模板](docs/templates/FEATURE_SPEC.md) 明确结果、非目标、失败语义、平台差异和 Harness 验收。

## 11. 已冻结的实施基线

1. 官方 v1 逐函数目录、稳定 ID、原子/标准实现层级、结果类型、宿主要求与目标能力矩阵以 [函数系统](docs/vnext/FUNCTIONS.md) 为准。结构控制不是函数；页面、存储、租约和任意 Shell 不进入首版目录。
2. 扩展清单 `easycode-extension.json`、贡献点、可信本地代码边界、密封运行产物和 `easycode.lock` 线格式以 [扩展平台](docs/vnext/EXTENSIONS.md) 与 [项目格式 6](docs/vnext/PROJECT_FORMAT.md) 为准。
3. 项目函数只以格式 6 的 ProgramDocument 保存。旧画布、格式 5 与 `.easy` 不迁移；不存在可编辑源码或平权文本数据源。
4. 用户程序通用重试只存在于异常区域的可选策略中；不存在独立重试语句、函数或散布到每个函数的通用重试字段。

这些基线已经关闭会改变实现路线的开放问题。实现必须按垂直切片和 Harness 证据推进；若未来改变任一基线，先新增或取代 ADR，再修改对应需求与验收，不能由局部代码静默偏离。
