# EasyCode 设计规范

> 版本：3.0  
> 更新日期：2026-08-26  
> 视觉方向：Grok-inspired Graphite（暖黑石墨 + 克制橙色 + 专业桌面密度）

## 0. 使用规则

本文件是 EasyCode IDE、Player、原生捕获宿主及后续新页面的唯一视觉基线。

- 新建页面或组件前必须先阅读本文件。
- 优先复用 `frontend/src/components/controls/`、Element Plus 主题变量和全局 `--app-*` Token，不为单页另造一套视觉语言。
- 禁止在业务组件中随意硬编码颜色、阴影、圆角和字号；确有新语义时，先补充本规范和全局 Token。
- 所有功能图标统一使用 Lucide。Emoji 只能作为用户内容，不能充当按钮、状态或导航图标。
- 本规范借鉴 Grok 的暖中性色、橙色强调、低装饰和内容优先布局，但不照搬聊天产品的大留白。EasyCode 是高信息密度的桌面 IDE，默认采用紧凑而不拥挤的专业密度。
- 默认主题为深色。文档中的浅色 Token 是未来浅色主题的映射基线，不允许组件自行判断主题并写死颜色。

参考资料：

- [Grok UI screenshots / DesignShots](https://design-sites.botubot.ru/project/grok-web/)
- [Grok UI examples / Nicelydone](https://nicelydone.club/apps/grok)
- [Grok screens / Lazyweb](https://www.lazyweb.com/company/grok)

## 1. 设计原则

1. **内容优先**：画布、节点、属性和运行状态是主角；装饰不能抢夺注意力。
2. **安静分层**：主要依靠相邻明度、1px 边框和留白区分层级，不使用大面积渐变和常驻重阴影。
3. **单一强调色**：橙色只表达主操作、当前选择和焦点；成功、警告、失败保持独立语义色。
4. **稳定胜于炫技**：拖拽、缩放、捕获和高频日志区域不使用模糊、复杂滤镜或大范围动画。
5. **密度有序**：编辑器控件以 28–32px 为主，Player 以 36–40px 为主；同一区域不得混用多个密度。
6. **状态完整**：每个可交互组件必须具有默认、悬停、按下、焦点、选中、禁用、加载和错误状态。

## 2. 配色

### 2.1 品牌与强调色

| Token | 色值 | 用途 |
| --- | --- | --- |
| `--app-color-primary` | `#D95417` | 主按钮、当前导航、选中描边、焦点 |
| `--app-color-primary-hover` | `#E66A31` | 主色悬停 |
| `--app-color-primary-pressed` | `#B94312` | 主色按下 |
| `--app-color-primary-soft` | `rgba(217, 84, 23, 0.14)` | 选中背景、轻提示背景 |
| `--app-color-on-primary` | `#FFF8F2` | 主色按钮上的文字和图标 |

橙色不得用于大面积面板背景，也不得同时承担“成功”语义。

### 2.2 深色主题（当前默认）

| Token | 色值 | 用途 |
| --- | --- | --- |
| `--app-bg-base` | `#0F0F0E` | 应用最底层、画布外围 |
| `--app-bg-chrome` | `#121211` | 顶栏、状态栏、固定 Dock |
| `--app-bg-sidebar` | `#151514` | 左右侧栏 |
| `--app-bg-panel` | `#1A1A18` | 工具面板、卡片 |
| `--app-bg-raised` | `#20201E` | 弹窗、菜单、悬浮工具栏 |
| `--app-bg-input` | `#111110` | 输入框、编辑器字段 |
| `--app-bg-hover` | `#262623` | 列表与工具按钮悬停 |
| `--app-bg-selected` | `rgba(217, 84, 23, 0.14)` | 当前项和轻量选中 |
| `--app-border-subtle` | `rgba(255, 251, 242, 0.08)` | 面板分隔和弱边框 |
| `--app-border-default` | `#30302C` | 表单、卡片、菜单边框 |
| `--app-border-strong` | `#41413B` | 强调分隔、悬停边框 |
| `--app-text-primary` | `#F7F7F2` | 标题、主要内容 |
| `--app-text-regular` | `#D8D8D0` | 正文、表单值 |
| `--app-text-secondary` | `#9C9C93` | 辅助说明、次级信息 |
| `--app-text-placeholder` | `#6D6D66` | 占位符、弱提示 |
| `--app-text-disabled` | `#555550` | 禁用内容 |

### 2.3 浅色主题映射（预留）

| Token | 色值 | 用途 |
| --- | --- | --- |
| `--app-bg-base` | `#FBFBF3` | 暖白主背景 |
| `--app-bg-chrome` | `#F5F5ED` | 顶栏、状态栏 |
| `--app-bg-sidebar` | `#F1F1E9` | 左右侧栏 |
| `--app-bg-panel` | `#FFFFFF` | 面板、卡片 |
| `--app-bg-raised` | `#FFFFFF` | 弹窗、菜单 |
| `--app-bg-input` | `#FAFAF5` | 输入框 |
| `--app-bg-hover` | `#EFEFE7` | 悬停 |
| `--app-border-subtle` | `rgba(26, 26, 26, 0.08)` | 弱分隔 |
| `--app-border-default` | `#E1E1D8` | 默认边框 |
| `--app-border-strong` | `#C9C9BF` | 强边框 |
| `--app-text-primary` | `#1A1A1A` | 标题、主要内容 |
| `--app-text-regular` | `#3D3D39` | 正文 |
| `--app-text-secondary` | `#70706A` | 辅助内容 |
| `--app-text-placeholder` | `#999990` | 占位符 |

### 2.4 语义色

| 语义 | 主色 | 轻背景 | 规则 |
| --- | --- | --- | --- |
| 成功 | `#3BA66B` | `rgba(59, 166, 107, 0.14)` | 执行成功、成功端口、已保存 |
| 警告 | `#D89A32` | `rgba(216, 154, 50, 0.14)` | 可继续的风险、暂停 |
| 失败 | `#D84A4A` | `rgba(216, 74, 74, 0.14)` | 执行失败、失败端口、破坏性操作 |
| 信息 | `#5C83C7` | `rgba(92, 131, 199, 0.14)` | 运行中、普通说明 |

语义色必须同时配合 Lucide 图标或文字，不得只依赖颜色传达结果。

## 3. 字体

### 3.1 字体族

```css
--app-font-sans: Inter, "SF Pro Text", "SF Pro Display", "Segoe UI Variable",
  "PingFang SC", "Microsoft YaHei UI", sans-serif;
--app-font-mono: "JetBrains Mono", "SFMono-Regular", Consolas,
  "Liberation Mono", monospace;
```

- 普通界面使用无衬线字体栈。
- 日志、表达式、代码、ID、路径和坐标使用等宽字体。
- 中文与英文混排不使用全大写正文；快捷键和极短标签可例外。

### 3.2 字号层级

| 层级 | 字号 / 行高 | 字重 | 用途 |
| --- | --- | --- | --- |
| Display | `24px / 32px` | 600 | 欢迎页主标题；一个页面最多一个 |
| Page title | `18px / 26px` | 600 | 独立中心页、Player 页面标题 |
| Section title | `15px / 22px` | 600 | 弹窗标题、大区块标题 |
| Panel title | `13px / 20px` | 600 | 工具窗口、属性分组标题 |
| Body | `13px / 20px` | 400 | 默认正文、表单值 |
| Body medium | `13px / 20px` | 500 | 菜单项、按钮、重要字段 |
| Compact | `12px / 18px` | 400/500 | 节点参数、列表辅助信息 |
| Caption | `11px / 16px` | 400 | 路径、时间、状态栏、帮助文案 |
| Micro | `10px / 14px` | 500 | 端口序号、极短徽标；不得承载正文 |

- 默认只使用 `400 / 500 / 600` 三档字重。
- 标题使用轻微负字距：`-0.01em` 至 `-0.02em`；正文保持 `0`。
- 不使用 9px 或更小字号；不依靠粗体堆叠制造层级。

## 4. 间距与留白

采用 4px 基础网格：

| Token | 值 | 常见用途 |
| --- | --- | --- |
| `--space-0-5` | `2px` | 紧邻状态点、像素修正 |
| `--space-1` | `4px` | 图标组、紧凑列表间距 |
| `--space-1-5` | `6px` | 图标与文字、紧凑按钮 |
| `--space-2` | `8px` | 默认控件间距、列表内边距 |
| `--space-3` | `12px` | 面板工具栏、表单行间距 |
| `--space-4` | `16px` | 面板内容内边距、卡片内边距 |
| `--space-5` | `20px` | 分组间距 |
| `--space-6` | `24px` | 弹窗区块、Player 模块间距 |
| `--space-8` | `32px` | 页面主区块间距 |
| `--space-10` | `40px` | 欢迎页和空状态留白 |

规则：

- 图标与标签间距：`6px`。
- 同一表单字段内：`6–8px`；相邻字段：`12px`；不同表单分组：`20px`。
- IDE 面板内容默认 `12px`，复杂表单可用 `16px`。
- Player 卡片内容默认 `20px`，页面左右安全边距 `24px`。
- 空状态使用留白而不是大面积插画；主说明与辅助说明间距 `6px`。

## 5. 圆角、边框与阴影

### 5.1 圆角

| Token | 值 | 用途 |
| --- | --- | --- |
| `--radius-xs` | `4px` | 端口标识、微型徽标 |
| `--radius-sm` | `6px` | 紧凑按钮、输入框、列表项 |
| `--radius-md` | `8px` | 节点、普通卡片、菜单项 |
| `--radius-lg` | `12px` | 弹窗、欢迎页内容卡、Player 卡片 |
| `--radius-xl` | `16px` | 仅用于大型 Player 主卡或启动页 |
| `--radius-pill` | `999px` | 状态胶囊、开关；不用于普通按钮 |

### 5.2 边框

- 默认边框为 `1px solid var(--app-border-default)`。
- 面板之间使用 `1px solid var(--app-border-subtle)`，不叠加双边框。
- 当前选中使用 `2px` 主色描边或 `1px` 主色边框 + `3px` 柔和焦点环，二者取其一。
- 分割器常态透明，悬停显示主色；可点击区域至少 `4px`。

### 5.3 阴影

| Token | 值 | 用途 |
| --- | --- | --- |
| `--shadow-sm` | `0 2px 8px rgba(0, 0, 0, 0.22)` | 浮动工具条、小弹出层 |
| `--shadow-md` | `0 12px 32px rgba(0, 0, 0, 0.34)` | 下拉菜单、Popover |
| `--shadow-lg` | `0 24px 64px rgba(0, 0, 0, 0.46)` | 模态弹窗 |
| `--focus-ring` | `0 0 0 3px rgba(217, 84, 23, 0.20)` | 键盘焦点、输入焦点 |

- 固定侧栏、普通卡片、节点卡片默认不使用重阴影。
- 阴影只表达悬浮层级，不能代替边框。
- 高频画布区域禁止 `backdrop-filter`；模糊只允许小面积低频弹出层使用。

## 6. 组件规范

### 6.1 按钮

| 尺寸 | 高度 | 水平内边距 | 图标 |
| --- | --- | --- | --- |
| 紧凑 | `28px` | `10px` | `14px` |
| 默认 | `32px` | `12px` | `16px` |
| Player/主行动 | `38px` | `16px` | `18px` |

- 主按钮：橙色背景、浅色文字，无渐变；每个视觉分组原则上最多一个。
- 次按钮：面板色背景 + 默认边框；悬停提升一个背景层级。
- 幽灵按钮：透明背景；悬停使用 `--app-bg-hover`。
- 危险按钮：常态保持中性或红色文字，确认阶段才使用实心红色。
- 纯图标按钮为正方形，必须提供 Tooltip 和 `aria-label`。
- 加载时保留按钮宽度，显示旋转图标并阻止重复提交。

### 6.2 输入框、选择器和表单

- IDE 默认高度 `32px`，紧凑属性表可用 `28px`；Player 默认 `38px`。
- 背景 `--app-bg-input`，边框 `--app-border-default`，圆角 `6px`。
- 悬停仅提高边框；焦点使用主色边框和 `--focus-ring`。
- 标签位于字段上方或稳定的双列网格左侧，不使用仅靠 placeholder 表达的标签。
- 帮助与校验信息放在字段下方，字号 `11px / 16px`。
- 错误必须写明可执行的修复方式，不能只写“失败”。

### 6.3 卡片与面板

- 普通 IDE 面板：纯色背景、1px 分隔、无阴影、外圆角 `0–8px`（取决于是否贴边）。
- 独立卡片：圆角 `8px`、边框 1px、内边距 `16px`。
- Player 卡片：圆角 `12px`、内边距 `20px`，允许极轻阴影。
- 可点击卡片悬停只改变背景和边框，不整体上浮；避免画布与列表晃动。
- 面板标题栏高度 `36px`，水平内边距 `12px`，标题使用 `13px / 20px / 600`。

### 6.4 导航栏与标签页

- 顶部菜单栏高度 `40px`；调试工具栏高度 `36px`；状态栏高度 `24px`。
- 左右 Activity Bar 宽度 `44px`，图标按钮 `32 × 32px`。
- 侧栏列表行高 `32px`，树节点最小高度 `28px`。
- 当前项使用柔和橙色背景 + 2px 内侧标记，不使用整块高饱和填充。
- 标签页高度 `34px`；当前标签用文字提亮和 2px 橙色下划线。
- 导航图标统一 Lucide `16–18px`、`stroke-width: 1.8`。

### 6.5 菜单、Popover 与 Tooltip

- 菜单内边距 `4px`，菜单项高度 `30px`，圆角 `6px`。
- 菜单分隔符左右至少保留 `8px`。
- Popover 圆角 `10px`、默认边框、`--shadow-md`。
- Tooltip 仅放简短提示或快捷键，不承载操作流程。
- 右键菜单应与顶栏菜单使用同一组件和视觉 Token。

### 6.6 弹窗

- 圆角 `12px`，背景 `--app-bg-raised`，边框 1px，`--shadow-lg`。
- 标题区 `16px 18px 12px`；内容区 `16px 18px`；操作区 `12px 18px 16px`。
- 普通弹窗宽度建议 `480–640px`；复杂编辑器可为 `760–960px`。
- 主操作位于右下，取消在其左侧；关闭使用 Lucide `X`。
- 破坏性操作必须说明影响对象和是否可恢复。

### 6.7 列表、树与表格

- 紧凑行高 `28px`，默认行高 `32px`，数据表行高 `36px`。
- 悬停、选中、键盘焦点必须区分；多选项在画布和节点管理器同步高亮。
- 路径、ID、时间和数值列使用等宽字体或 tabular numbers。
- 空、加载、错误三种状态分别设计，不允许统一显示为 `No Data`。

### 6.8 图标与状态反馈

- 仅使用 `lucide-vue-next` 或原生宿主中的同款 Lucide SVG Path。
- 常规图标 `16px`，标题图标 `18px`，主入口 `20px`；默认线宽 `1.8`。
- 不以 `❓`、`✅`、`❌`、`⚠️` 等 Emoji 作为 UI 图标。
- Toast 只反馈短期结果；需要用户处理的错误必须在相关区域持续显示。
- 运行中、成功、暂停、失败使用“图标 + 文字 + 语义色”，不能只显示颜色点。

## 7. EasyCode 专用界面

### 7.1 IDE 主壳层

```text
顶部菜单 40
调试工具栏 36
┌─Activity 44─┬─左侧工具窗 240–360─┬─中央画布─┬─右侧检查器 300–420─┬─Activity 44─┐
│             │                     │          │                    │             │
└─────────────┴─────────────────────┴──────────┴────────────────────┴─────────────┘
底部工具窗 160–360（按需展开）
状态栏 24
```

- 中央画布永远拥有最高空间优先级；侧栏可折叠、可调整宽度。
- 同层面板贴边时不使用卡片圆角和阴影，只用分隔线。
- 顶栏不承载所有动作；低频操作进入菜单，高频运行/捕获动作进入调试工具栏。

### 7.2 画布与节点

- 画布使用 `--app-bg-base`，网格线与背景明度差不超过 6%。
- 节点背景 `--app-bg-raised`，默认边框 `--app-border-default`，圆角 `8px`。
- 节点选中：2px 橙色描边；多选节点和左侧节点列表同步选中。
- 当前执行节点使用信息蓝或成功绿的动态状态标记，不与普通选中橙色混淆。
- 成功端口为绿、失败端口为红、普通端口为中性灰；端口编号必须有可读文字或 Tooltip。
- 连线常态降低对比，选中或执行时提亮；拖线时禁止触发非必要图片预览刷新。
- 节点拖动期间关闭阴影动画、图片重算和非必要过渡，以帧率优先。

### 7.3 属性检查器

- 使用紧凑表单密度，字段高度 `28–32px`。
- 参数按“核心 / 识别 / 重试与超时 / 高级”分组；高级参数默认折叠。
- 每种 Control 组件必须共享统一标签、帮助、错误和禁用状态。
- 图片预览按需加载、缓存；拖动画布和折叠面板时不得反复请求。

### 7.4 资源管理器

- 左侧目录树默认宽 `220–260px`，始终显示 `image / ocr / page` 三个根分类。
- 当前默认目录只决定首次选中位置，不能限制用户切换分类。
- 缩略图卡片圆角 `8px`，图片区域使用棋盘格或中性底，文件名位于下方。
- 选择、录入、捕获保存必须复用同一资源管理器与目录树组件。
- 删除、移动和重命名操作必须展示引用影响，并同步节点与资源元数据。

### 7.5 原生截图与控件捕获

- 捕获宿主以性能为第一约束，不使用 WebView 模糊或大面积阴影。
- 有效截图区域使用 2px 橙色高亮；区域外使用 `rgba(0, 0, 0, 0.52)` 遮罩。
- 工具栏背景 `#1A1A18`，圆角 `8px`，1px 边框，按钮 `32 × 32px`。
- 工具栏全部使用 Lucide 图标，当前可用与禁用状态必须清晰。
- 从属性字段进入的一次性捕获仅显示“清空 / 取消 / 确认”；确认使用 `Check`，取消使用 `X`。

### 7.6 Player

- Player 面向脚本使用者，密度比 IDE 舒展一级：控件高 `38px`、卡片内边距 `20px`、模块间距 `24px`。
- 首屏优先展示脚本名称、目标环境状态、必要参数和唯一主运行按钮。
- 高级配置、开发者日志和诊断信息默认折叠。
- 开发者暴露的项目设置和节点参数沿用同一表单组件体系，不另造 Player 样式。
- 运行状态需要明确显示准备、运行、暂停、恢复、成功、失败，不只输出日志。

## 8. 布局与响应式

- IDE 最小建议窗口：`1280 × 720`；低于此尺寸优先折叠侧栏，不压缩中央画布到不可用。
- 独立设置页和欢迎页内容最大宽度 `960px`；长正文最大行宽 `72ch`。
- 面板使用 CSS Grid/Flex 和可调整 Splitter，不使用绝对定位拼装主布局。
- 窄窗口中，次要按钮收进“更多”菜单；核心运行、停止、保存状态不可隐藏。
- 在 Windows 100%、125%、150% 缩放下验证，不依赖单一 DPI 的像素对齐。

## 9. 动效与性能

| Token | 时长 | 用途 |
| --- | --- | --- |
| `--motion-fast` | `100ms` | 按下、悬停 |
| `--motion-normal` | `160ms` | 菜单、Popover、折叠 |
| `--motion-slow` | `220ms` | 模态进入、页面级切换 |

- 默认缓动：`cubic-bezier(0.2, 0, 0, 1)`。
- 画布拖动、节点拖动、截图框选、滚动日志不添加补间动画。
- 禁止 `transition: all`，只列出确实变化的属性。
- 尊重 `prefers-reduced-motion: reduce`。
- 高频区域禁用大面积 `backdrop-filter`、发光阴影和持续脉冲。

## 10. 文案与交互反馈

- 按钮使用“动词 + 对象”：打开项目、新建节点、停止任务。
- 错误格式：发生了什么 + 影响 + 用户下一步。例如：“目标窗口已最小化，无法捕获完整画面。请先还原窗口后重试。”
- 保存采用自动保存；状态栏显示“正在保存 / 已保存 / 自动保存失败”。不以频繁成功 Toast 打扰用户。
- 长任务展示可取消进度；网络或后端请求超过 300ms 才显示加载，避免快速闪烁。
- 不可用操作应禁用并通过 Tooltip 解释条件，不能点击后才报错。

## 11. CSS Token 基线

全局主题应以以下语义 Token 为准，Element Plus 变量只做映射层：

```css
:root {
  color-scheme: dark;
  --app-color-primary: #D95417;
  --app-color-primary-hover: #E66A31;
  --app-color-primary-pressed: #B94312;
  --app-color-primary-soft: rgba(217, 84, 23, 0.14);
  --app-color-on-primary: #FFF8F2;

  --app-bg-base: #0F0F0E;
  --app-bg-chrome: #121211;
  --app-bg-sidebar: #151514;
  --app-bg-panel: #1A1A18;
  --app-bg-raised: #20201E;
  --app-bg-input: #111110;
  --app-bg-hover: #262623;

  --app-border-subtle: rgba(255, 251, 242, 0.08);
  --app-border-default: #30302C;
  --app-border-strong: #41413B;

  --app-text-primary: #F7F7F2;
  --app-text-regular: #D8D8D0;
  --app-text-secondary: #9C9C93;
  --app-text-placeholder: #6D6D66;

  --app-color-success: #3BA66B;
  --app-color-warning: #D89A32;
  --app-color-danger: #D84A4A;
  --app-color-info: #5C83C7;

  --radius-sm: 6px;
  --radius-md: 8px;
  --radius-lg: 12px;
  --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.22);
  --shadow-md: 0 12px 32px rgba(0, 0, 0, 0.34);
  --shadow-lg: 0 24px 64px rgba(0, 0, 0, 0.46);
  --focus-ring: 0 0 0 3px rgba(217, 84, 23, 0.20);
}
```

## 12. 新页面交付检查清单

每个新页面或重大改版在提交前逐项检查：

- [ ] 已阅读 `design.md`，只使用现有语义 Token；没有无理由硬编码颜色。
- [ ] 图标全部来自 Lucide，没有 Emoji 或混用其他图标库。
- [ ] 字号、行高、间距落在规定层级和 4px 网格上。
- [ ] 主操作唯一明确；危险操作有二次确认和影响说明。
- [ ] 默认、悬停、按下、焦点、选中、禁用、加载、空、错误状态齐全。
- [ ] 键盘 Tab 顺序合理，焦点可见，纯图标按钮有 Tooltip/`aria-label`。
- [ ] 画布列表、属性检查器和关联导航的选中状态保持同步。
- [ ] 请求无重复提交；加载不闪烁；失败可恢复且给出下一步。
- [ ] 在 1280×720、1920×1080 与 Windows 125%/150% 缩放下完成视觉检查。
- [ ] 拖拽、缩放、滚动、捕获和日志高频更新时没有明显掉帧。
- [ ] IDE 与 Player 共用组件的页面分别检查，不能只验证其中一端。

## 13. 产品信息架构（强制）

### 13.1 一级区域

EasyCode 只允许以下六个一级区域，新增功能必须先归入其中之一：

| 区域 | 职责 | 允许内容 | 禁止内容 |
| --- | --- | --- | --- |
| 欢迎页 | 进入工作区 | 打开项目、新建项目、最近项目 | 项目配置、营销卡片、假统计 |
| 顶部命令栏 | 全局命令 | 文件/编辑/视图/运行/发布、保存状态、搜索、工作面板、捕获 | 工作区模式、面包屑、常驻大按钮、日志、详细表单 |
| 左侧上下文栏 | 工作区导航与持续编辑上下文 | 主流程、函数、页面地图、资源、变量和运行入口 | 函数契约、能力详情、复杂配置表单 |
| 中央编辑区 | 当前主要任务 | 画布、复杂能力工作台、帧回放、资源管理器 | 重复的全局导航 |
| 右侧检查器 | 当前对象的属性 | 单节点、多选批量属性、函数契约、暂停时变量 | 项目级低频设置 |
| 底部面板 | 辅助输出 | 运行日志、可关闭的诊断输出 | 永久说明、配置表单 |

### 13.2 入口分级

1. **工作区与持续上下文**放 Activity Bar：主流程、函数、页面地图、资源、变量、运行和日志。
2. **高频全局动作**放命令栏：搜索、工作面板和捕获；工作区切换不得在顶部重复出现。
3. **低频简单动作**放菜单或右键菜单：导入、导出、模板、自动布局。
4. **低频复杂任务**打开中心工作台或大弹窗：能力库、运行服务、帧回放、Player 配置、资源管理。
5. **对象动作**优先靠近对象：树节点右键、画布节点右键、行悬停动作。
6. 同一动作最多保留两个入口：一个可发现入口和一个效率入口（快捷键/右键）；不得三处重复。

### 13.3 当前命令地图

| 命令 | 主入口 | 效率入口 | 可用条件 |
| --- | --- | --- | --- |
| 打开/新建项目 | 欢迎页、文件菜单 | 无 | 未运行 |
| 全局搜索与引用 | 顶栏搜索、编辑菜单 | `Ctrl+P` | 已打开项目 |
| 从当前节点执行 | 运行菜单 | `F5` | 只选中一个业务流程节点且预检通过 |
| 暂停/继续/单步/停止 | 会话调试条 | `F6/F5/F10/Shift+F5` | 存在执行会话 |
| 视觉捕获 | 顶栏捕获菜单 | `Alt+Q` | 项目已打开且未运行/暂停 |
| 控件捕获 | 顶栏捕获菜单 | `Ctrl+Shift+C` | 项目已打开且未运行/暂停 |
| 新建函数 | 左侧函数库 | 函数空工作区主操作 | 已打开项目且未只读 |
| 打开函数画布 | 左侧函数列表单击 | Enter/Space | 已存在函数 |
| 查看函数契约 | 打开函数且画布未选节点时的右侧检查器 | 无 | 已打开函数 |
| 能力库 | 视图菜单 → 开发工具 | 无 | 已打开项目 |
| 运行服务 | 运行菜单 → 开发工具 | 无 | 已打开项目 |
| Player 配置/发布 | 发布菜单 | 无 | 已打开项目 |
| 版本历史 | 文件菜单 | 无 | 已打开项目 |

## 14. IDE Shell 精确规范

### 14.1 顶部命令栏

- 高度 `42px`，单层布局，不再叠加空闲调试栏。
- 从左到右：品牌、主菜单、保存状态、搜索、工作面板和捕获；不展示项目/流程/节点面包屑或画布模式切换。
- 宽度不足时先收起次要文字标签，再压缩搜索框；工作区导航始终留在 Activity Bar，不进入顶栏。
- “从当前节点执行”不做常驻绿色按钮；运行是一项有前置条件和较高影响的命令，保留在运行菜单和 `F5`。
- 自动保存是状态而非主要操作：保存成功使用弱绿色，保存中用警告色，失败持续显示红色并允许点击重试。
- 录制状态只在录制期间出现；静止界面不预留空位。

### 14.2 Activity Bar 与工具窗口

- Activity Bar 宽 `40px`，按钮 `32×32px`；左栏 Tooltip 向右，右栏向左。
- 左侧结构域常驻主流程、函数和页面地图，资产/运行入口按领域分组；右侧常驻属性、函数契约和调试变量；日志入口贴近左侧底部。
- 主流程、函数和页面地图图标是唯一工作区导航，点击时保持左栏展开并切换内容；其他工具入口可再次点击收起。
- 工具窗口默认宽度：左 `280px`，右 `340px`；拖拽范围分别为 `200–520px`、`280–620px`。
- 面板标题由 `ToolWindow` 统一承担；业务组件不得再重复一遍同名标题。
- 面板关闭后保留宽度、滚动位置和展开状态；流程/拓扑分别记忆资源树状态。

### 14.3 状态栏

- 高度 `24px`。
- 左侧只展示执行状态与临时会话状态；右侧只展示项目名和当前画布模式。
- 禁止展示框架版本、字符编码、全路径、重复保存状态、无变化计数。
- 正常空闲状态最多四段信息；异常信息优先覆盖普通信息。

## 15. 项目资源树

- 顶部项目行只保留：提取为流程、新建节点文件夹、更多。不得提供会创建空流程的“新建流程”。
- 项目根级直接展示主画布中的节点与节点文件夹；内部稳定主流程不显示成额外层级。
- 可复用流程统一收纳到“流程库”分区。只有通过“提取为流程”、导入含节点的流程包或应用含节点的流程模板才能创建。
- 导入流程包和流程模板进入“更多”；搜索独占下一行。
- 节点文件夹是当前画布内的编辑器树节点，不使用横向 Chip 带；删除文件夹时节点回到当前画布根级，不删除节点。
- 流程库、可复用流程、节点文件夹和节点使用缩进轨道表达层级；画布不绘制流程或文件夹包围框。
- 拖动手柄只在悬停/拖动时增强；节点类型图标默认中性，主选中节点才使用强调色。
- 单击、`Ctrl+单击`、`Shift+单击`、框选的结果必须与画布和检查器同步。
- `↑/↓/Home/End` 移动焦点，`←/→` 折叠、展开或进入子级，`Enter/Space` 激活，`F2` 重命名，菜单键或 `Shift+F10` 打开对象菜单。
- 树使用 roving tabindex，任何时刻只有一行进入 Tab 顺序；浮层关闭后焦点归还原行。
- 拖拽显示插入线或目标文件夹背景；删除有确认且支持撤销。文件夹右键菜单统一承载重命名与删除，不直接触发破坏性确认。
- 资源树统一使用固定行高虚拟化和前后 `8` 行 overscan；1000 节点时 DOM 行数应小于 50。构造文件夹分组必须为线性复杂度，不得为每个文件夹重复扫描全部任务。
- 未进入视口的图片预览不得刷新；过滤只重建扁平索引，不挂载完整树。

## 16. 属性检查器与 Schema 表单

### 16.1 节点检查器结构

固定顺序：

1. 节点摘要：类型图标、节点名称、类型名、稳定 ID。
2. 节点配置：Schema 渲染的主要参数。
3. 节点专属反馈：OCR 识别文本、图片预览或测试结果，仅在相关节点出现。
4. 高级配置：超过 12 项或低频参数时折叠；折叠行展示覆盖项数量/关键摘要。
5. 固定底栏：自动保存状态、延迟、循环。

节点名称只出现一次。类型是不可编辑说明，不伪装为字段。ID 默认弱化，只用于定位问题。

### 16.2 响应式字段布局

属性面板以自身容器而不是浏览器宽度响应：

| 容器宽度 | 布局 |
| --- | --- |
| `<420px` | 全部标签在上、控件全宽 |
| `420–519px` | 仍以上下布局为主；坐标、路径、映射等复杂控件全宽 |
| `>=520px` | 简单字段可用 `minmax(112px,.42fr) minmax(0,1fr)` 双列；复杂字段仍上下 |

- 所有字段必须有持久标签；placeholder 只给格式示例，不能替代标签。
- 表单修改同步写入内存，`400ms` 合并保存；失焦、Enter、切换节点、切换画布和关闭窗口前必须 flush。
- 数字字段必须有单位或语义后缀；默认值由后端 Schema 提供，前端不得另造不一致默认值。
- 依赖条件隐藏字段时保留数据，除非产品语义明确要求清空。

### 16.3 Placeholder 规则

| 字段 | 合格示例 | 不合格示例 |
| --- | --- | --- |
| 变量名 | `例如 run_count` | `请输入` |
| 窗口标题 | `例如 雷电模拟器` | `窗口` |
| 区域 | `x, y, 宽, 高` | `请输入识别区域` |
| 表达式 | `例如 $var{retry_count} + 1` | `表达式` |
| URL | `例如 http://192.168.1.10:8000` | `协调器地址` |

## 17. 变量、能力与运行服务

### 17.1 变量

- 使用用户变量、运行上下文、系统变量三个标签页；搜索时跨分类混合结果并显示来源标签。
- 行结构固定为：表达式 + 来源/类型；第二行显示值和说明；复制/编辑/删除只在悬停或键盘聚焦时出现。
- Token、路径、数值使用等宽字体；不为每个变量绘制独立卡片，不为类型配置五花八门的图标。
- 双击用户变量编辑；系统/上下文变量只读且支持复制。

### 17.2 能力库

- 属于中心工作台，不放在 280px 常驻侧栏。
- 宽屏为“目录 + 契约详情”两栏；窄屏先列表，详情转独立层。
- 分类为项目能力、公共能力、平台内置；目录和详情必须来自 `/api/capabilities`，不得前端写死假能力。
- 创建向导只负责脚手架与契约，创建后重新扫描并选中新能力。

### 17.3 运行服务

- 与能力库共用“开发工具”容器；内部再分概览、计划、状态、消息、租约、局域网。
- 概览只显示后端计数；无数据时显示空状态，不显示演示数值。
- 消息、租约、局域网属于专家功能，字段必须解释作用域、所有者、TTL 和失败语义。
- Player 复用同一组件，但使用 `scope=player` 和当前实例 ID，不能读取 IDE 最近项目。

## 18. 画布与调试

- 新项目默认收起运行日志；开始执行时自动展开，用户仍可随时手动收起。空闲编辑态不应长期牺牲画布高度。

- 画布永远是 IDE 中央主任务；面板展开不得改变画布坐标语义。
- 流程/拓扑共用交互模型，但数据、选中、撤销栈、展开和滚动状态独立。
- 画布空白右键仅含新建节点和可用时的粘贴；端口拖到空白创建节点后必须自动连线。
- 多选拖动保持相对位置；复制只包含所选节点及两端均在选择内的连线；粘贴生成新 ID。
- 执行节点高亮与断点高亮独立；执行中不要求节点具有断点才显示。
- 调试工具条只在运行或暂停出现。按钮顺序为状态、暂停/继续、单步、停止、断点摘要、当前节点。
- 日志默认关闭；打开后占底部水平空间。清空、复制、过滤等操作位于日志面板头部，不进入全局顶栏。

### 18.1 运行日志

- 默认展示全部日志，按来源提供固定分类：执行、节点、识别、导航、操作、数据；“问题”跨分类聚合警告和错误。
- `category` 是后端日志契约的一部分并随执行记录落库；前端只为旧日志和本地临时日志做兼容推断，禁止在组件中散落另一套分类规则。
- 分类只改变当前视图，不删除、降采样或修改原始日志；复制始终复制完整日志，清空始终清空完整日志。
- 不显示“自动滚动 / 手动滚动”长文字开关。默认跟随最新日志；用户向上滚动时自动暂停，底部浮出“回到最新”和未读数量，点击后恢复跟随。
- 工具栏左侧为分类及数量，右侧只保留 Lucide 复制和清空图标；图标必须有 Tooltip、`aria-label`、禁用和键盘焦点状态。
- 日志行固定为时间、来源、严重级别和消息。时间使用等宽 tabular numerals，来源使用弱化短标签；警告和错误同时使用不同图标、文字颜色和可访问名称。
- 日志继续使用固定行高虚拟化，过滤发生在虚拟窗口之前；空日志与当前分类无结果必须分别展示空状态。
- 面板窄于 `760px` 时隐藏每行重复的来源列，分类工具栏允许横向滚动，复制和清空操作始终固定在右侧。

## 19. 资源管理器与捕获

- 所有图片选择和保存复用同一资源管理器；`image/ocr/page` 永远可见，调用方只决定默认目录。
- 选择模式与保存模式共享目录树、路径、缩略图、加载、空、错误状态；保存模式额外显示文件名和冲突策略。
- 从属性字段发起捕获是一次性任务：清空、退出、确认；确认后回填并退出。
- 全局视觉捕获是连续编辑任务：生成节点成功后清空点/框，保留冻结帧，直到 Esc。
- 捕获悬浮层必须使用原生宿主保证速度和桌面坐标；文件管理器仍复用 Vue 业务组件。
- 截图范围外使用遮罩并拦截指针；最小化窗口在进入前阻止；坐标始终相对工作面板。
- 原生宿主与 Vue 层采用同一尺寸、圆角、图标和语义 Token，但不为视觉一致牺牲捕获热路径性能。

## 20. Player

- Player 是运行产品，不复制 IDE 导航。
- 顶部只保留标题、实例选择、增加/删除实例、运行/恢复/停止、原生窗口控制。
- 运行状态只在状态条展示一次；标题栏不重复状态徽标。
- 入口与配置方案在次级工具条；环境自检可直接访问，计划、运行服务和诊断包收进“更多”。
- 主体在 `>=920px` 时为参数/日志双栏；低于该宽度改为纵向，参数在前、日志在后。
- 空表单文案为“当前脚本无需额外运行参数”，不得暴露 `form_schema` 等内部字段名。
- 日志使用纯文本状态，不显示装饰 Emoji；错误应包含节点、原因和建议动作。
- Player 主操作高度 `38px`；禁用停止按钮而不是点击后提示“未运行”。
- IDE、Player、Capture 分别按需注册 Element Plus 组件；构建不得把三入口强制合并为一个 UI vendor 块。Player 只能预加载其运行、参数、日志和运维入口实际依赖的控件。

## 21. 弹窗、中心工作台与复合功能

选择承载方式的判定：

| 内容 | 承载方式 |
| --- | --- |
| 单次确认、少于 8 个字段 | 普通弹窗 `480–640px` |
| 多步骤创建 | 向导弹窗，底部固定上一步/下一步 |
| 需要列表与详情、长期停留 | 中心工作台 `960–1080px` |
| 与当前字段直接相关的小选择 | Popover/下拉菜单 |
| 快捷全局命令 | 命令面板 |

- 弹窗不得嵌套弹窗；必须嵌套的资源选择使用同一顶层 Teleport 和明确返回路径。
- 复杂工作台侧导航只显示领域，内容区显示详情；导航文字在 `<760px` 可折叠为图标。
- 破坏性确认必须展示对象名、影响和恢复方式；可撤销时优先使用 Undo Toast 而不是多次确认。

## 22. 内容减负与真实数据规则

每个展示项必须至少满足一个条件：帮助做决定、解释当前状态、提供操作、支持定位问题。否则删除。

必须删除或禁止：

- 框架版本、编码、无变化计数、重复路径和重复状态。
- 仅为了填满卡片的说明、示例统计、假趋势、假插件、假能力。
- 空闲时不可操作的整条工具栏。
- 同一行超过三个无层级的图标按钮；低频动作进入“更多”。
- 内部模型名、字段名、HTTP 状态码直接作为用户文案。

数据来源规则：

- 后端数据未返回时显示 Skeleton/加载；返回空数组显示空状态；请求失败显示错误和重试。
- 前端不得用演示数组作为生产兜底。开发 fixture 只能位于测试文件或显式 Story/Playground。
- 每个网络型组件必须记录 API、请求条件、加载/空/错误/成功状态和取消策略。

## 23. 可访问性与键盘

- 所有图标按钮使用真实 `button`，提供 `aria-label` 和 Tooltip。
- 标签页使用 `role=tablist/tab` 和 `aria-selected`；树使用可辨识的展开状态。
- `Tab` 顺序遵循视觉顺序；弹窗打开后聚焦标题后的第一个可编辑字段，关闭后归还触发器。
- 菜单打开后聚焦首个可用项；`↑/↓` 循环、`Home/End` 跳转首尾、`Esc` 关闭。禁用项不进入方向键序列。
- 图片资源卡片使用 `role=option` 和 `aria-selected`；Space 选择，Enter 首次选择、再次确认。
- 断点入口必须是 `button`，命中区域不小于 `24×24px`，通过 `aria-pressed` 表达是否已设置。
- `Esc` 关闭最上层非破坏性浮层；捕获和录制优先消费 Esc 并明确状态。
- 文字与背景普通对比不低于 4.5:1；大字/非文本控件不低于 3:1。
- 不仅靠颜色表达成功/失败/选中，必须同时有文字、形状或图标。
- Windows 125%、150%、200% DPI 下点击目标保持至少 `28×28px`，Player 主目标至少 `36×36px`。

## 24. 开发与验收协议

### 24.1 组件优先级

1. 先查 `frontend/src/components/controls/`。
2. 再查共享 Shell/面板/空状态/图标按钮。
3. Element Plus 只作为行为基础，视觉由全局 Token 覆盖。
4. 只有一种新交互会在至少两个位置复用时才新建通用 Control；否则先保持局部。
5. 业务组件不得复制资源管理器、捕获、窗口选择、变量表达式或条件编辑逻辑。

### 24.2 响应式验证矩阵

| 场景 | 必测尺寸 |
| --- | --- |
| IDE | 1280×720、1440×900、1920×1080 |
| 窄窗口 | 900×700、720×640 |
| Player | 1024×720、1366×768、1920×1080 |
| DPI | 100%、125%、150%、200% |
| 属性容器 | 320、420、520、620px |

### 24.3 自动化验收

- Vue 单测：所有共享 Control、菜单命令、面板可见条件、Player 运维入口。
- 前端构建：IDE、Player、Capture 三入口全部构建成功。
- API 合约：前端 API 路径均被 FastAPI 路由承接，且请求/响应关键字段一致。
- 画布 E2E：主流程新建节点、连线、拖动、多选、函数创建/调用/返回、页面地图、复制粘贴、撤销重做、工作区导航和自动保存。
- 视觉回归：欢迎页、IDE 全布局、每类检查器、资源管理器、能力/运行工作台、Player、所有弹窗。
- 性能：拖动画布时非选中节点不刷新预览；长列表虚拟化；日志 DOM 有上限；后台轮询可见性降频。

### 24.4 文档治理

- `design.md` 是唯一规范源；代码行为改变后同一提交必须更新对应章节。
- 新增一级入口、通用组件、尺寸 Token 或响应式断点必须先更新文档。
- 设计图是说明材料，不是规范源；设计图与本文冲突时以本文为准。
- 每次大版本记录“保留/移动/合并/删除”的入口决策，防止无意恢复历史冗余。

## 25. 外部设计技能的适用边界

EasyCode 的外部设计参考使用 Impeccable、Design Taste Frontend 与 Redesign Existing Projects。它们是审计和思考工具，不是高于产品目标的模板。

### 25.1 规则优先级

1. 用户明确需求与已确认的产品语义。
2. EasyCode 的大型脚本搭建、调试、发布和 Player 使用目标。
3. 本文档的组件、信息架构、交互与验收规范。
4. 桌面 IDE 的 Operate 模式惯例。
5. 外部技能中与以上规则兼容的建议。

发生冲突时不得机械套用外部默认值。明确保留 Vue、Pinia、Element Plus、Lucide 和原生捕获宿主；禁止因技能示例迁移到 React/Tailwind、替换图标体系、引入营销页 Hero、大图叙事或强滚动动效。

### 25.2 EasyCode Design Read

| 维度 | 定义 |
| --- | --- |
| 产品模式 | Operate：高频、专业、键鼠优先、长期使用 |
| Design Variance | 4/10：Shell 与表单克制，画布与捕获允许产品辨识度 |
| Motion Intensity | 2/10：只解释状态和空间关系 |
| Visual Density | 8/10：高密度，但通过层级、折叠、搜索和按需展示维持可读性 |

任何新页面在实现前都要写清楚：核心对象、主任务、第一操作、加载/空/错误/成功状态、键盘路径、窗口与容器宽度降级方式。

### 25.3 审计门槛

- 每次大型 UI 迭代按 Accessibility、Performance、Theming、Responsive、Implementation Integrity 五维评分，总分 20。
- P0/P1 问题不得带入发布；P2 必须进入明确迭代；P3 可集中清理。
- 核心菜单、项目树、资源卡片和检查器动作必须有完整键盘路径，不得仅绑定在不可聚焦的 `div/span`。
- 500 个节点以上必须启用规模保护；资源树和日志使用虚拟化，非可视节点暂停预览与非必要计算。
- CSS 动画禁止 `transition: all`，禁止通过 `width/height/padding/margin` 制造悬停位移；优先使用 `transform` 和 `opacity`。
- 普通 UI 颜色必须使用主题 Token；画布端口、节点类型、成功/失败等领域色必须集中到命名语义映射中。
- 生产构建必须记录三入口体积；单个 vendor 块超过 500 kB 时需要说明原因和按需加载计划，不能只调高警告阈值。
- 审计报告保存到 `docs/`；当前基线见 `docs/UI_EXTERNAL_SKILL_AUDIT.md`。

### 25.4 审计命令

```powershell
node "$env:USERPROFILE/.codex/skills/impeccable/scripts/context.mjs" --target frontend/src/App.vue
node "$env:USERPROFILE/.codex/skills/impeccable/scripts/detect.mjs" --json frontend/src
cd frontend
npm run build
```

扫描器结论必须结合代码上下文人工复核。领域色、SVG 属性和桌面端紧凑点击目标不能直接按营销网页规则判错。

## 26. 2.1 实施记录

本轮遵循 Impeccable、Design Taste Frontend 与 Redesign Existing Projects 的审计流程，但以 EasyCode 的 Operate 模式和高密度桌面交互为准。

- 键盘：画布菜单、项目树、图片卡片与断点入口完成语义化和焦点闭环。
- 规模：项目资源树改为扁平索引 + 固定行高虚拟窗口；画布继续沿用既有视口裁剪，两者分工明确。
- 构建：移除 IDE 全量 Element Plus 插件注册和强制单块分包，三个入口均按实际组件集合注册；生产构建不得出现超过 `500 kB` 的单块。
- 主题：菜单、调试状态、Player 状态、Activity Bar Tooltip、画布选中和端口颜色统一使用 `--app-*` 语义 Token。
- 动效：业务 CSS 禁止 `transition: all`；卡片悬停不再上浮，按下反馈只允许 `1px` 且不改变布局。
- 视觉：悬浮层使用不透明暖石墨背景、1px 边框和中等阴影；高频画布与菜单不叠加高成本模糊。

验收证据记录在 `docs/UI_EXTERNAL_SKILL_AUDIT.md`。任何后续回归都必须同时更新本文与对应自动化测试。

## 27. v3 项目对象与画布架构（3.0）

### 27.1 对象语义

| 对象 | 运行语义 | 编辑器语义 | 持久化位置 |
| --- | --- | --- | --- |
| 主流程 | 项目唯一日常执行入口，不可被调用 | 普通脚本搭建画布 | `workflow.json.main_graph` |
| 函数 | 显式参数、局部变量、输出与结果出口的可调用单元 | 每个函数拥有独立画布和测试用例 | `workflow.json.functions[]` |
| 调用函数节点 | 按稳定 ID 传入实参、接收输出并路由结果 | 主流程或其他函数中的普通节点 | 所在画布 `nodes[]` |
| 能力 | 由代码实现、可安装和版本化的复杂能力包 | 能力库展示真实契约；通过能力调用节点使用 | 项目/公共能力目录与调用节点 |
| 页面地图 | 项目唯一、扁平的页面状态和导航图 | 独立页面地图画布 | `topology.json` |

不再提供任务、子流程、流程模板、流程包、节点文件夹、页面集合、跨任务连线、几何区块或“选中流程提取为函数”。复用逻辑只通过显式函数；复杂代码能力只通过能力库；大型画布通过搜索、筛选、自动布局和小地图整理。

### 27.2 前端入口与工作区

- Activity Bar 的“结构”域包含主流程大纲、函数和页面地图；“资产”域包含资源、能力和变量；“运行”域包含会话、计划、诊断与多实例服务。
- 打开项目默认进入主流程。Activity Bar 是主流程、函数与页面地图的唯一工作区入口；顶部不重复提供模式切换或上下文面包屑。
- 单击“函数”先进入空函数工作区；再单击函数列表项，中央自动展示对应函数画布，无需额外的“打开画布”按钮。
- 左侧函数面板只承担搜索、目录和函数列表；右侧检查器展示当前函数的形参、局部变量、输出、结果出口和测试。选中节点时右侧优先展示节点属性，清空选择后返回函数检查器。
- 主流程和页面地图侧栏使用可搜索的扁平大纲；节点选择、画布高亮和大纲高亮保持同一状态源。
- 常用创建入口放在相应侧栏标题栏和画布右键菜单；全局命令、导入导出、历史和发布进入顶栏菜单或命令面板。
- 所有通用图标使用 Lucide；纯图标按钮必须同时具有 Tooltip、`aria-label`、焦点、禁用和按下状态。

### 27.3 函数工作区与检查器

- 函数入口只负责切换工作区；未选中函数时中央显示轻量空状态，并提供“新建函数”主操作。
- 函数列表单击即打开函数画布，双击不承担隐藏的第二层语义；键盘 Enter/Space 与单击一致。
- 函数列表、函数画布和函数检查器共享当前函数 ID，不复制临时选择状态。
- 右侧检查器优先级固定为：单节点属性、多节点批量属性、当前函数契约、空状态。
- 函数契约修改与节点属性一样进入统一自动保存队列；切换函数、切换工作区、运行和关闭前必须 flush。
- 删除当前函数后回到空函数工作区；删除被调用函数时先阻止并展示引用数量。

### 27.4 函数契约与调用

- 函数只能从函数库入口显式创建；创建时生成固定函数入口和至少一个显式返回节点。
- 函数契约包含稳定的 `parameter_id/local_id/output_id/outcome_id`。显示名可修改，调用绑定仍依赖稳定 ID。
- 参数通过 `$param.name` 或 `$param{中文名}` 只读访问；局部变量通过 `$local.name` 访问；输出只能在返回节点中显式绑定。
- 每个函数至少包含“成功”和不可删除的系统异常出口；调用函数节点据此生成稳定的彩色输出端口。
- 调用节点必须展示缺失实参、无效输出目标和失效函数引用；删除仍被调用的函数时阻止并给出引用数量。
- 函数禁止直接或间接递归；运行时维护调用栈和深度限制，异常沿系统异常出口或失败状态传播。
- 每个函数可保存多个测试用例，直接测试使用隔离上下文，不修改正式主流程；结果展示出口、输出、日志和异常。
- 函数导出为数据型 `.ecf`，包含所需函数闭包、稳定资源和能力依赖；导入时为所有对象生成新 ID 并重写内部引用，不执行包内代码。

### 27.5 能力与页面地图

- 能力适合纯节点难以表达的算法、协议或平台集成，不与函数混为同一对象。能力有真实版本、输入、输出、权限和运行环境契约。
- 项目能力、公共能力、平台内置能力在同一能力库分组展示；扫描、创建、复制 ID 和失败重试均由真实 API 承接。
- 页面地图为项目级唯一扁平图，只允许页面状态和导航操作相关节点；所有页面可直接连线和参与全局寻路。
- 页面弹窗是页面节点自身属性，不由区块名称、所在位置或函数决定；智能跳转只在执行到对应节点时启用页面地图。
- 发布前检查从主流程入口递归收集函数、资源、能力和页面地图依赖；缺失稳定 ID、失效绑定、递归调用或缺失资源必须阻止发布。

### 27.6 保存、删除和恢复

- v3 项目只接受严格的 `project.json/workflow.json/topology.json/context.json/form_schema.json`，不猜测迁移旧字段。
- 所有编辑先更新内存，再以 `400ms` 合并自动保存；失焦、Enter、切换节点、切换画布、关闭和运行前必须 flush。
- 多文件修改使用可恢复事务和原子替换；保存失败保留用户内存状态并给出具体文档与字段，不得用旧磁盘数据覆盖画布。
- 删除节点同步删除关联边和无效绑定；删除资源同步清理节点引用和捕获元数据；删除函数、能力或页面前先检查引用。
- 历史快照属于项目文件入口，不属于视图入口；恢复后重新加载全部 v3 文档并清空过期撤销栈。
- 画布树和菜单支持方向键、Home/End、Enter/Space、F2、Shift+F10；右键菜单的每个动作都有键盘等价入口。

## 28. 大型画布的连线可读性与路由规范（3.1）

### 28.1 新建节点入口与提交语义

- 画布工具栏必须提供常驻的 Lucide `CirclePlus` 新建节点入口；画布空白处右键同样提供“新建节点”。
- 从空出口拖到空白处，打开“选择新建节点类型”菜单；保存成功后原子生成节点与连线。出口已有连线时，拖到空白处只断开原线，不打开新建菜单。
- 新节点只有在后端保存成功后才成为当前主选节点，并自动定位到视口；保存失败必须回滚节点和边，不得留下“数据里存在、画布看不到”的半提交状态。
- 新节点 ID 必须使用 UUID 级独立标识；显示名按同类型最小可用序号去重，不能以时间戳碰运气。

### 28.2 聚焦与执行状态

- 悬停一个节点时，高亮它的所有入边和出边；选中一个或多个节点时，内部边与边界边保持清晰，无关边降至 `14%` 不透明度。
- 多选集合中的内部边优先级高于边界边；执行中的边优先级高于所有编辑态，不得被降噪隐藏。
- 普通边始终静止。只有后端明确上报 `previous_node_id → current_node_id` 时，该真实执行边显示流动虚线动画。
- 任务结束、停止或会话清理后必须同时清空前后节点状态，避免画布残留伪执行动画。

### 28.3 自动路由、微车道与 Line Jump

- 自动路由使用网格 A*，节点矩形及一格安全间距为硬障碍；已占用通道和潜在垂直交叉作为软成本，后生成的边优先选择更空闲的走廊。
- 完全或大段重合的内部线段使用稳定微车道分离，车道按稳定 `edge_id` 排序，刷新、撤销和重新打开项目后不得左右抖动。
- 垂直交叉且不存在端点连接时，稳定排序靠后的边绘制 Line Jump：先用画布背景擦出缺口，再画小桥。相交、共线和真实连接点不得误画桥。
- 通道分析必须按轴线和空间桶分组，不允许对全图所有线段做无条件 O(S²) 扫描。
- 本阶段不实现总线收束：EasyCode 的稳定端口通常只有一个真实出口，强制捆绑会掩盖运行语义。

### 28.4 手动转接点

- 双击连线在鼠标投影位置添加转接点；右键连线提供“添加转接点”和“重置自动路由”。
- 选中转接点后可拖动；方向键按一个 `20px` 网格微调；Delete/Backspace 删除当前转接点。转接点只有在编辑对应边时显示。
- 转接点落入节点安全区时，提交阶段搜索最近空闲网格自动避让；无可用位置时删除该点并恢复该段自动路由。
- 同时移动连线两端节点时，内部转接点按同一位移整体移动；只移动一端时保留世界坐标，再做碰撞避让。
- 复制子图时只复制所选节点之间的内部边及其转接点，并为边和转接点生成新 ID；粘贴时转接点随子图落点偏移。
- 每次增删、拖动、微调和重置都必须进入同一画布撤销栈，并通过 Edge Schema 的 `routing.mode/waypoints[]` 持久化。Player 和执行器忽略该编辑器字段，但不得在加载、保存、导出时丢弃它。

### 28.5 性能门槛与 ELK 引入条件

- 节点拖动热路径只绘制轻量正交预览；松手后再恢复 A*、通道成本、微车道与 Line Jump。
- 几何路由和视觉状态分离：选择、悬停、断点、执行状态改变不得重新运行 A*。
- A* 结果按节点几何、边集合和转接点签名缓存；缓存上限受控，图结构变化时自然失效。
- 画布继续使用视口裁剪；不可见边不进入 SVG DOM，但路由占用统计仍以完整图为准，避免滚动画布时路线跳变。
- 以 `100 / 500 / 1000` 节点的稀疏、分支和高交叉三类图做分层测试。单次交互不得长时间阻塞主线程，拖动帧内不得执行全图自动布局。
- 只有真实大图测试证明“节点初始排布导致的交叉”仍是主要瓶颈，且现有显式自动布局无法解决时，才评估 Web Worker 中的 ELK Layered。ELK 不得在拖动、选中或普通保存时自动运行。
