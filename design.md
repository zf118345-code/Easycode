# EasyCode vNext UI 设计与开发规范

跨工作区、扩展贡献页面及 Windows/Android Player 的结构、状态、视觉度量和响应式硬约束，以 [统一界面系统](docs/vnext/UI_SYSTEM.md) 为细分权威规范；本文件中的页面示例不得覆盖该规范。

> 版本：5.4
>
> 状态：Approved（总体工作台、结构化编辑、值表达式与 Control 交互方向已冻结；具体视觉数值、响应式阈值与真实性证据仍由实现和 Harness 验证）
>
> 更新日期：2026-09-07
>
> 适用范围：IDE、Player、原生捕获宿主以及 Android Player

## 1. 文档治理

本文件是 EasyCode 前端信息架构、交互、视觉与 Control 实现的权威基线。规格冻结为 `Approved` 后，新增页面、修改组件或调整交互必须先读取本文；规则变化时，规格、实现和 Harness 在同一垂直切片中同步更新。

项目函数的唯一可编辑事实是版本化强类型 `ProgramDocument`。纵向语句编辑器、检查器、变量视图、Player 绑定和调试器都通过结构化命令引用或修改同一模型。只读伪代码、语句摘要、问题文本和运行日志均为派生表示，不得保存为第二份业务数据。

旧节点画布、任务组、流程连线、可编辑 `.easy` 源码、Monaco、解析草稿和旧 Schema 不属于 vNext。页面导航、页面拓扑、随机弹窗扫描和 `页面.*` 函数不属于首个正式版本；`page` 只保留为通用资源目录。

任何进入界面的内容至少帮助用户完成一项真实任务：创建程序、理解上下文、修改真实数据、管理扩展、运行交付或诊断问题。无后端承接的按钮、演示数字、装饰性图标、假插件入口和未实现平台入口不得进入产品。首版不提供公开扩展市场、评分、付费或远程代码安装。

## 2. 产品与交互原则

1. **结构化模型是地基**：界面可以变化，ProgramDocument、稳定 ID、类型和 ECIR 边界不能由局部组件绕过。
2. **先框架后细节**：先冻结产品边界、领域对象、运行与扩展边界，再讨论工作区、Control、文案和视觉微调。
3. **一个动作一个主入口**：高频动作就近显示，低频动作进入对象级菜单；禁止同一功能在多个位置竞争。
4. **渐进披露**：默认只展示完成当前任务所需的信息；高级、危险和低频配置按区块或独立工作区展开。
5. **真实反馈**：保存、校验、编译、运行、捕获、扩展和发布显示真实状态，不使用模拟进度或无后端数据。
6. **不丢工作**：已提交结构命令事务保存；Control 内尚未提交的输入草稿在切换、失焦、关闭和保存冲突时有明确提交、保留或恢复语义。
7. **键盘优先但不排斥鼠标**：插入、选择、移动、嵌套、删除、撤销、调试和检查器编辑均有完整键盘路径。
8. **安静的专业密度**：用层级、边界和留白建立秩序，不靠大卡片、重阴影、渐变和五花八门的图标。
9. **跨端语义一致**：Windows、ADB 和 Android 本机只改变目标驱动、捕获宿主与可用动作，不改变 ProgramDocument 和 Player 绑定模型。
10. **信息必须挣得位置**：常驻信息必须帮助用户立即判断状态、选择对象或完成当前动作。同一事实只保留一个主要展示位置；实现名、稳定 ID、哈希、路径、来源和历史等追溯信息默认进入提示、技术信息或诊断，不与业务名称和可操作状态平级。无法回答“用户现在据此做什么”的文字、徽标、计数和空面板不得常驻。

信息按用户任务分为三级：

- **首要信息**：当前对象、当前状态、下一步动作、阻断原因与恢复入口，始终直接可见。
- **辅助信息**：会改变用户选择的用途说明、适用目标、权限和已设置状态，就近显示但降低视觉权重。
- **追溯信息**：稳定 ID、实现类型、宿主模块、超时、路径、哈希、帧空间版本和文件大小，默认收进“技术信息”或“开发者信息”；展开后仍完整可复制。

安全警告、权限影响、破坏性操作、错误原因和恢复动作不得以“信息克制”为理由隐藏。渐进披露只降低低频信息的常驻密度，不删除功能，也不制造第二条更难发现的操作路径。

## 3. 信息架构

IDE 固定为四层：

1. 顶部标题栏：品牌、完整应用菜单、弹性留白、检查、调试和运行状态操作。应用菜单承接低频但完整的命令发现路径；当前页面只保留高频或强上下文动作。工作区与当前函数已经在活动栏、目录和中央标题中明确时，不再常驻重复面包屑。
2. 左侧活动栏：程序、资源、Player、运行目标、运行记录、计划与实例及启用的真实功能扩展。活动栏可在“图标”和“图标 + 名称”两种投影间切换；问题、输出和当前值共用底部反馈面板，不再伪装成另一个工作区。
3. 主工作区：按任务选择结构。程序与 Player 可以使用导航—编辑/预览—检查器；资源使用目录—内容—按需详情；目标、变量、计划和扩展不得为了形式统一强行补齐三栏，只有存在独立选择、主要任务和对象配置三种真实职责时才拆分。
4. 底部区域：单一反馈面板，以标签切换运行输出、当前值和问题；最底部为紧凑状态栏。

活动栏职责：

- **程序**：项目函数与函数目录。左侧使用“项目 / 官方 / 已启用扩展”标签；中央是当前项目函数的纵向结构化语句投影；右侧是当前语句或待插入函数的检查器。
- **资源**：左侧目录、中间网格/列表预览、右侧元数据与引用。
- **Player**：左侧字段与区块树、中间实时预览、右侧字段配置。
- **运行目标**：Windows 窗口/桌面、Android ADB 和 Android 本机；任何宿主只有真实实现后才出现。
- **运行记录**：录制会话、历史帧时间线、比较、离线分析、导出和本机空间管理；不与运行输出或资源目录混成一个列表。
- **计划与实例**：本地计划、批次、当前设备实例和已配对局域网实例；只展示真实可调度对象与权限。
- **问题/输出**：真实诊断和运行事件，可在底部展开；活动栏只显示有效计数。
- **功能扩展工作区**：仅在项目启用并真实贡献工作区后出现；首版使用中立测试扩展验证地基，不预留页面入口。

扩展安装与维护不是独立“市场”入口。项目菜单/项目设置中的“管理扩展”打开真实管理工作区，负责导入、启用、禁用、锁定、更新、卸载、权限和引用影响。普通函数目录只展示已注册、可调用的扩展函数，不展示实现文件。

项目默认一个入口函数；业务片段按需提取为项目函数。函数之间通过调用形成层级，面包屑、转到定义和查找引用提供方向感。项目函数为空时是合法可编辑对象，但无引用、无内容的临时函数可在用户确认后清理，不承担旧“节点容器”职责。

## 4. 视觉令牌

### 4.1 字体

```css
--font-ui: "Inter", "Segoe UI Variable", "Microsoft YaHei UI", sans-serif;
--font-mono: "JetBrains Mono", "Cascadia Code", "Microsoft YaHei UI", monospace;
```

- 主工作区/弹窗标题：15px / 600 / 21px。
- 正文、表单标签、列表、区块标题、辅助说明、状态栏、徽标与语句摘要：13px / 400–600；使用颜色、字重和间距表达层级。
- 同一行的标题、计数、状态和动作文字保持同字号；空间不足时截断或布局回落，不缩小其中一项。中央摘要的固定文本、半方括号动态值和步骤备注共享同一行高与文字基线；动态值仅使用半方括号区分，文字本身继承所在语句的字体、字号、字重与颜色。
- 欢迎页品牌标题、二维码配对码和移动端防自动缩放输入是任务性例外；路径、哈希、ID 和需逐字符比较的数据可以使用等宽字体，但不因此缩小字号。
- 不使用全大写英文标题，不用字间距模拟高级感。

### 4.2 深色主题

```css
--app-bg-base: #0f0f0e;
--app-bg-chrome: #121211;
--app-bg-sidebar: #151514;
--app-bg-panel: #1a1a18;
--app-bg-raised: #20201e;
--app-bg-input: #111110;
--app-bg-hover: #262623;
--app-bg-active: #30302c;

--app-text-primary: #f7f7f2;
--app-text-regular: #d8d8d0;
--app-text-secondary: #9c9c93;
--app-text-placeholder: #6d6d66;

--app-border-subtle: rgba(255, 251, 242, .08);
--app-border-default: #30302c;
--app-border-strong: #41413b;

--app-color-primary: #d95417;
--app-color-primary-hover: #e66a31;
--app-color-primary-dim: rgba(217, 84, 23, .14);
--app-color-success: #3ba66b;
--app-color-warning: #d89a32;
--app-color-danger: #d84a4a;
--app-color-info: #5c83c7;

--focus-ring: 0 0 0 3px rgba(217, 84, 23, .20);
```

橙色只用于当前选择、主动作、焦点与少量品牌识别。成功、警告和失败只承载状态语义。正文对背景对比度至少 4.5:1，非文本控件边界至少 3:1。

### 4.3 间距、尺寸与圆角

采用 4px 基础网格：4、6、8、12、16、20、24、32。

- 标题栏 40px；状态栏 24px；工作区标签 34px。
- 活动栏 44px；左侧栏默认 260px；右侧检查器默认 340px。
- IDE 输入控件 30–34px；Player 输入控件 36–40px。
- 普通语句行最低 32px；区块标题 34px；紧凑列表行 30px；两行函数项 44px。
- 紧凑按钮 28–30px；主要对话框按钮 32–34px。
- 小控件圆角 5–6px；弹窗 10px；浮层 8px。禁止泛用 14–20px 大圆角。
- 主界面不使用阴影；仅对话框、菜单、工具提示和桌面捕获工具栏使用阴影。

结构化编辑专用令牌至少包含语句悬停、选中、多选、当前执行、错误、断点、插入标记、作用域轨道和拖动目标状态；其颜色必须从以上语义令牌派生。

## 5. 应用外壳

### 5.1 顶部标题栏

从左到右为品牌、`项目 / 编辑 / 视图 / 运行 / 帮助` 应用菜单、弹性留白、检查、调试和运行状态操作。只有进入计算聚焦、引用定位等用户确实可能迷失方向的深层任务时，才在该任务内部显示上下文路径；顶栏不重复“项目 / 当前工作区 / 当前函数”。窄窗口把应用菜单收进一个有可读名称的总菜单，不得把菜单项直接挤出可访问区域。

应用菜单是完整功能索引，不是另一排常驻工具栏：项目生命周期、撤销重做、剪切复制粘贴、工作区和面板切换、运行调试、快捷键设置、诊断与帮助必须能从对应菜单发现；高频动作可以同时拥有就近按钮和快捷键。菜单项显示当前有效快捷键，禁用时原位说明原因，不能点击后才提示“暂不可用”。快捷键由稳定命令 ID 绑定，可在 EasyCode 内搜索、修改、恢复默认和检测冲突；普通文本输入仍遵循平台通用编辑快捷键。

运行按钮不使用大面积绿色；普通状态使用克制描边。执行中只保留与当前状态有效的继续、单步、暂停和停止。禁用按钮直接禁用并用工具提示解释原因，不能点击后才弹错误。窄窗口可以隐藏动作文字，但必须保留图标、可读名称和工具提示。

保存状态使用短文案：等待保存、正在保存、已保存、存在冲突、保存失败。它表示 ProgramDocument/项目事务状态，不表示“文本是否通过解析”。

### 5.2 左侧活动栏

图标统一 Lucide、19px，按钮 32px。活动栏由同一数据清单投影为两种模式：紧凑模式宽 44px，只显示图标并在悬停/聚焦时解释；文字模式显示图标和稳定工作区名称，面向初次使用者。顶部品牌/项目图标切换模式，“视图”菜单提供等价命令；偏好属于当前设备用户，不写入项目。默认中性色，当前视图用弱橙背景；问题数量使用小型语义徽标。每个按钮有 `aria-label`、`title` 和键盘导航。

点击工作区按钮仍只负责工作区切换与该左栏展开/收起，不能同时承担模式切换。窄宽下左栏使用覆盖抽屉，活动栏可临时只投影图标，但不能修改用户偏好。

扩展贡献的活动栏入口必须有稳定贡献 ID、真实启用状态和卸载回收路径。不得让扩展任意插入一组视觉不一致的图标或绕过活动栏排序规则。

### 5.3 侧栏与检查器

侧栏标题高 40px。所有工作区组合同一 `NavigationPane / PaneHeader / ListToolbar / ObjectListItem`：标题、有效备注、动作、32px 搜索、28px 紧凑行、背景、悬停、选中和计数不允许逐页重写。列表优先使用无边框行，不给每个条目套卡片；当前项统一使用弱橙背景和左侧 2px 标识。父级名称位于展开图标之后，子级继续向右缩进且字号不大于父级。`NavigationPane` 标题动作全部使用 28px 纯 Lucide 图标，并提供准确 tooltip 与可访问名称；正文和工作区主标题中的文字动作不受此限制。

活动栏同时承担工作区切换和左侧栏开关：点击其他工作区切换并展开其左栏，再次点击当前图标收起或展开。所有工作区共享同一左栏宽度；低于 1100px 时左栏改为覆盖式抽屉，不挤压中央内容。程序检查器在最右侧使用对称的检查器活动按钮展开或收起，窄宽时同样以抽屉覆盖；窄屏左右抽屉必须互斥，打开一侧自动收起另一侧，不能共同遮住正文。

低于 1100px 默认收起检查器，通过覆盖式抽屉恢复。资源、Player 和扩展工作区使用同一响应式模式，禁止直接隐藏属性入口。宽度持久化必须提供键盘替代与重置，不通过挤压控件形成难读的两列窄表单。

工作区不强制套用同一个左中右三栏。程序与 Player 设计可使用目录/主区/检查器；资源、变量、目标、扩展、计划等以对象选择和属性编辑为主的页面默认使用列表/详情两栏；运行记录按会话/时间线与预览组织。设置、快捷键、发布审核等需要集中处理但不需要长期占满工作台的任务使用 EasyCode 内的大型模态工作区，保留统一标题、导航、搜索、确认和焦点规则，不调用 Windows 通用确认框替代产品界面。

所有长期面板使用可复用分隔条调整尺寸：鼠标或触摸拖动，键盘方向键微调，Home 恢复该面板默认值。尺寸按项目写入 `.easycode/view-state.json` 的 `panel_sizes`，损坏或越界时回到安全默认值；响应式抽屉不覆盖用户保存的桌面尺寸。底部问题/日志面板同样可调整高度并保留最后尺寸。

## 6. 纵向结构化语句编辑器

中央编辑区按执行顺序投影当前 ProgramDocument。它不是文本编辑器，也不是自由画布；普通语句默认一行，条件、循环、异常、“切换目标并执行”和监听使用缩进、区块标题与作用域轨道表达。内部仍使用 `target_scope`，用户无需理解作用域术语。

行内快速插入同时承担键盘意图输入：输入普通关键词时继续返回同一函数目录，输入“查找并判断”“重复”“执行直到”等结构意图时预览将创建的普通结构。确认只提交一次可撤销 Program Service 事务；意图文字不持久化，也不成为可编辑源码。项目函数参数与返回类型从当前函数标题的就近入口编辑，不塞进某条语句检查器。

普通语句行不在左侧重复绘制函数、赋值等通用类型图标；动作文字已经表达类型。图片等能直接帮助辨认实际参数的缩略图可以保留，运行、断点、问题和失败状态使用专属状态标记。控制结构依靠关键词、折叠入口、缩进和作用域轨道区分，不用每行不同图标制造视觉字典。

流程正文只使用一种层级语法：普通语句是内容行；条件、循环、异常和目标切换是范围标题；“那么 / 否则如果 / 否则 / 捕获 / 最后”是范围内的轻量分隔行。范围标题可以使用极弱的中性底色，分隔行使用短标签与细分隔线，不得做成另一种普通语句卡片。作用域轨道只从嵌套层级开始并跨过分支分隔行保持连续，根级不绘制装饰竖线。目标切换可保留一个帮助识别上下文变化的专用图标，其余控制类型不建立逐类图标或颜色字典。空分支的添加入口继续可见和可聚焦，但视觉权重必须低于已存在的分支。

同一语句只允许一套用户词汇：目录使用可插入的自然短动作，中央只补充会改变理解的真实参数，检查器直接沿用中央标题。不得在检查器标题上方再次显示“赋值、条件、异常区域、显式失败”等实现分类。稳定结构类型继续用于模型、命令、无障碍层级和诊断，但不与业务语义争夺界面位置。异常处理对用户统一称为“尝试”，分支使用“错误类型时 / 最后”；“执行里面的语句”“无论成功或失败”等已由层级表达的解释不进入常驻流程。无内容的可选分支只显示弱化添加入口，不伪装成已存在内容。

必须支持：

- 单选、Ctrl/Cmd 多选、Shift 连续选择、折叠和展开。
- 在明确插入位置创建函数调用、控制结构和变量赋值。
- 循环体内可插入“跳出循环”和“跳过本轮”；其他位置保留可发现但禁用的入口并解释“请先把插入位置放入循环”。“结束为失败”可在任意语句块插入。
- 删除、复制、剪切、粘贴、上下移动、移入/移出作用域和拖拽重排。
- 撤销、重做、提取为项目函数、转到定义、查找引用和稳定重命名。
- 断点、当前指令、已完成、等待、失败和暂停状态。
- 大型 ProgramDocument 虚拟化；不可见折叠分支不实例化 Control 或昂贵预览。

中央编辑器使用行内快速插入器缩短高频编排路径。语句主选态提供“在此语句后插入”图标动作；选中行 `Enter` 或聚焦行 `Ctrl+Enter` 在实际提交位置展开一条 32px 搜索行，空函数直接显示首条搜索入口。搜索行和候选不伪装成 ProgramDocument 语句，不使用弹窗，也不要求用户再点击独立“插入”按钮；候选行本身可单击，键盘以方向键选择、`Enter` 确认、`Escape` 取消。

快速插入和左侧函数目录共享目录构建、关键词规范化、匹配和可插入状态，只允许查询状态与展示密度不同。快速入口跨控制结构、项目、官方和已启用扩展检索，按相关度显示紧凑结果；左侧仍承担分类浏览、查看详情和项目函数管理。快速插入期间不逐键访问后端，中文输入法组合键不会提交；失败保留搜索词与插入位置，成功后选择新语句并把焦点留在中央以便继续编排。

每条语句由函数契约/语句类型生成自动摘要，例如：

```text
 1  等待 2 秒
 2  等待图像「登录按钮」出现 · 超时 3 秒  → 登录结果
 3  点击「登录结果.中心」· 左键
 4  如果 登录结果.已找到
 5      输出日志「登录成功」
 6  否则
 7      输出日志「未找到登录按钮」
```

`step_label` 是可选备注，只在用户明确添加后显示；不得要求每条语句维护节点名称。摘要过长时保留动作、关键对象和关键差异值，次要参数进入检查器或悬停详情。复杂值摘要必须使用固定工作量和有界长度生成，不能递归拼接整棵树；子树存在错误时显示聚合数量，点击进入首个错误 `value_id`。

所有编辑产生版本化结构命令。移动保留 `statement_id`，复制生成新 ID；命令成功后投影更新。缺少必填参数的调用使用明确的未配置值状态，语句仍可选择、保存和修复，但运行与发布被语义诊断阻止。

正文中的目标范围不使用目标图标重复说明上下文；以克制的文字颜色与范围层级区分即可。图标保留给真实操作、状态和导航，不承担可由语句文字直接表达的装饰性分类。

异常区域的重试直接投影在区块标题中，例如“发生临时错误后最多再试 2 次 · 间隔 500ms”，不创建独立语句行或函数目录项。策略关闭时不显示冗余摘要；策略开启且区域含副作用时，标题显示克制警告状态，点击后聚焦检查器中的审核项。

“跳出循环”“跳过本轮”使用紧凑单行摘要，选中后检查器只说明它作用于最近一层循环，不展示伪参数。“结束为失败”显示错误消息摘要；检查器只编辑稳定错误 ID、消息和可选诊断详情，不把平台、实现分类或运行元数据混入表单。

条件结构只执行第一个满足的分支。中央依次显示“那么”和已有的“否则如果”；没有内容的否则不显示成“否则 暂无语句”，而是在分支末尾提供弱化的“添加否则执行的语句”插入动作。检查器把条件字段命名为“判断条件”，固定真/假分别显示“始终满足/永不满足”，入口使用“设置条件”，不把条件伪装成普通开关或要求用户输入公式。检查器提供“添加否则如果”，并允许直接移除空分支；非空分支必须先处理其中语句。需要多个条件各自独立触发时，使用多个“如果”，不使用单选分支。

诊断定位到稳定 `function_id + statement_id + value_id`，并附带可读字段路径。点击问题后选择语句、展开必要作用域并聚焦对应 Control。问题不使用无法看见的侧边红条作为唯一提示。

若未来提供只读伪代码，它必须标注“派生、只读”，不与主编辑器平权，不支持保存或反向导入。

## 7. 函数目录与扩展开发

函数实现分层：

- 官方原子函数：Python/原生/平台驱动实现的通用最小操作。
- 官方标准函数：ProgramDocument 组合出的高频方案，可有等价融合实现。
- 项目函数：当前项目用 ProgramDocument 组合出的业务流程。
- 扩展函数：项目级或用户级 Python/原生实现，经契约注册。

普通用户不按“原子/标准”寻找官方函数；官方目录只按图像、文字、输入、等待、日志、文件、消息等使用领域分组。规范显示名统一为 `命名空间.函数名`；自然语言别名只用于搜索和帮助。

官方目录可以比运行核心丰富：经过多项目验证的高频标准组合与原子能力使用完全相同的目录行、插入方式和检查器，不额外制造“快捷节点”入口。目录通过用途分组、搜索、别名和上下文推荐控制发现成本，而不是为了表面精简逼用户反复搭建常见组合。项目或行业专用方案留在项目函数或扩展中。

函数目录顶部固定“项目 / 官方 / 已启用扩展”标签和范围内搜索。控制结构不再占据标签上方的独立专区，而是在“官方”标签内按用户任务分类显示：判断、循环、尝试、目标范围、返回与结束归入“流程控制”；赋值在“变量”中拆成“新建局部变量”和“修改变量”两个任务入口，修改时必须从当前位置可见的局部变量或项目变量中选择；消息监听归入“消息”。这只是目录投影的合并；两个变量入口仍生成同一种强类型赋值结构命令，其他结构也不能改成普通函数调用。官方领域使用默认收起、同时只展开一个领域的手风琴，并记住上次展开位置；搜索期间自动展开全部命中领域。领域标题已提供命名空间时，行内只显示函数动作名，完整 `命名空间.函数名` 保留在可访问名称与提示中，避免重复噪声。官方、结构与扩展条目单击只选中并在检查器打开待插入配置，不能因浏览自动插入；双击、Enter 或行尾“插入”执行插入。项目函数按显示名称自然排序，单击或 Enter 始终打开编辑，插入只能使用行尾“插入”或菜单“插入调用”，不得把双击浏览变成隐藏写操作。项目函数提供打开、插入、查找引用、重命名和删除；官方函数只读查看和插入。

目录父级领域名称的字号不得小于子函数；子函数统一缩进到展开图标之后。左侧、中央和检查器使用同一份语义名称投影，不再为节省几个字制造第二套短名。

待插入配置是有边界的临时插入事务，不是第二份业务数据。确认后生成一个结构化 `call` 命令、稳定 `statement_id` 和类型化参数值；取消后丢弃临时事务。若用户选择快速插入，契约默认值直接填入，缺少值使用未配置状态。

EasyCode 不内置 Python 编辑器。扩展开发流程固定为：

1. 创建项目级或用户级扩展骨架。
2. 在文件管理器或配置的外部 IDE 中打开。
3. 回到 EasyCode 刷新实现签名。
4. 审核中文显示名、参数 Control、返回值、平台、权限、超时和依赖。
5. 运行契约测试并解决真实诊断。
6. 启用扩展并通过 Player 发布闭包检查。

外部 IDE 启动失败必须提供路径检查、重新选择和在文件管理器打开，不显示无反应按钮。扩展实现来源只在扩展维护工作区出现，不占用普通函数目录和检查器。

## 8. 右侧检查器与 Control

检查器只显示当前位置可以直接修改的内容。选中语句时编辑该语句完整参数、返回绑定和结构属性；选中函数目录项时编辑待插入调用。返回类型、平台列表、实现分类和长说明进入悬停文档或帮助抽屉，不常驻挤占参数区。

资源、回放、扩展、计划、Player 等真实三栏工作区共用外壳最右侧活动按钮控制详情栏；窄屏使用覆盖式抽屉，页面内部不再重复检查器开关。项目变量、运行目标等“列表 + 主编辑区”任务保持两栏，不为视觉对称制造空详情页。

调用返回绑定由一个输入框完成：留空不保存，普通名称新建局部变量，键入 `@` 或 `[` 后选择当前位置类型兼容的已有局部变量。项目变量不可作为写入目标；不存在要求用户预先理解的来源模式下拉。

所有表单的默认值与 placeholder 都必须表达可直接理解的真实答案、最终格式或可操作示例。能确定的默认行为直接显示实际语义，格式型字段显示目标格式，真正阻止运行的字段才标必填；禁止使用“默认配置”“无结果”“请输入内容”等不提供决策帮助的占位语。固定枚举继续使用普通下拉，不附加变量或计算入口。

检查器结构：上下文标题、参数/返回 Control、字段级错误、必要的高级区块和唯一主操作。函数调用的标题已经显示函数名称，因此不再加一行“函数调用”；结构语句仍可显示能区分语义的类型。无参数时只显示“无需配置参数”。系统级保存、扩展、捕获宿主或网络失败进入问题中心；当前字段只显示与该字段直接相关的错误。

选中异常区域时，检查器在普通捕获设置下提供默认关闭的“临时错误自动再试”。开启后先显示“最多再试次数”和“间隔”，默认分别为 2 次和 500ms；退避方式与异常分类进入高级设置。界面明确写出“首次执行不计入再试次数”“每次从区域开头重新执行”“已发生的点击、消息、网络和文件操作不会撤销”。若静态分析发现直接或传递副作用，检查器列出具体语句并要求作者确认；区域内容或锁定函数契约变化后确认自动失效。取消与停止不可配置为重试对象。

桌面表单默认使用最短的一行路径：`标签 | 32px Control | 同高字段动作`。标量、枚举、引用、简单表达式和紧凑 Record 叶字段只要仍能保留可辨认标签与可用输入宽度，就不得机械拆成上下两行；帮助与校验错误只在需要时归入该 Control 的下一行。仅多行正文、复杂集合，或内容区窄于 220px 后确实无法输入时回落为标签在上的全宽布局。Control 提交类型化结构命令，不序列化引号、逗号字符串或源码片段。

能在 Control 本体直接输入或选择的内容不得先经过摘要页、模式选择或第二张表单；能合并成一个字段事务的来源、返回绑定和条件不得拆成多个表单。条件 Control 本身承接布尔值、比较、且/或/非、变量、字段与候选补全，不再附加一个打开相同条件字段的“组合”按钮。资源选择和捕获作为同排字段动作存在。只有一行会损害理解、并且确实需要独立保存/取消事务的有界列表、字典、JSON 或深层 Record 才进入按内容定高的轻量聚焦层；不得仅因底层值是 Record 就打开大弹窗。

Control 至少支持：文本、整数、小数、时长、时间点、布尔、枚举、局部/项目变量引用、纯值表达式、坐标、区域、资源引用、目标引用、实例引用多选、路径点列表、结构化列表、字典和记录对象。每个 Control 统一提供：

- label、必填标识、当前值、placeholder、帮助、错误、禁用和加载状态；
- 参数标题下方的统一结构化表达式输入器；默认直接显示当前值、稳定引用词元或语义摘要，不再常驻“填写 / 变量 / 计算”三组来源按钮。点击或键入后按当前期望类型提供直接值、变量与结果、常用操作和完整目录，所有路径提交同一值树。
- 统一输入器递归覆盖函数参数、赋值、返回值、列表项、字典键和值、结构字段和计算输入。列表与字典使用多行容器管理增删和排序，每个可表达为自由标量的叶子继续复用统一输入器；嵌套集合与捕获型专用值进入对应 Control。枚举本身只显示受约束下拉，不显示变量或计算入口，也不能为了界面一致而退化为自由文本。
- 键盘输入、焦点环、中文 IME 和撤销友好提交；
- `blur`/Enter 的明确提交语义，多行文本使用 Ctrl/Cmd+Enter；
- 取点、框选、录图、取色、控件或路径捕获等平台动作，且动作能力由宿主真实声明。

文本 Control 只显示文本值，不显示持久化引号；列表多选提交 `list<string>` 值节点，不提交运行时再拆分的逗号文本。重复项、空项和契约上限就近报错。

枚举 Control 的中文标签、稳定值和平台范围只来自函数/类型契约，IDE、Player 与编译器不得各自维护选项副本。切换目标后，下拉框只提供当前平台可执行的选项；若已有值在新目标不可用，原位保留为禁用项并显示“当前目标不可用”，同时由编译检查阻止运行或发布，不能静默改成默认值。Windows 窗口点击默认使用不占用鼠标的定向后台路径；作者以图片、文字或页面条件验证业务效果，而不是把消息入队当作成功。`输入.输入文本` 的自动模式按目标选择后台/原生输入；Windows 可显式选择“仅后台输入”或经目标权限允许的“物理键盘”，ADB/Android 本机可显式选择“目标原生输入”。自动物理回退必须记录后台失败原因和真实输入路径。

`@` 候选只列当前作用域内的变量和结果本体，不把字段路径提前平铺；选中结构化值并输入 `.` 后，成员选择器才根据该值的类型契约列出当前层字段、中文名和字段类型，嵌套结构继续按 `.` 逐级进入，用户不手写或记忆字段字符串。可选结果统一提供“有结果”条件，不为图片、消息和控件分别发明底层存在语义；未确认有结果时仍让用户看见真实字段目录，但字段保持不可提交，检查器就近说明需先在产生结果的语句后添加“有结果”判断，进入成立分支后自动恢复可选。中央摘要可以按函数领域写成“找到”“收到”等自然中文，但它只是投影，不是记录字段。

一个值可以由直接内容、变量、结果、字段或纯计算组成，但这些来源只作为统一输入器中的候选分组和词元样式，不作为用户必须先理解的编辑模式。对象字段通过引用词元后的类型化字段候选进入。计算值只展示与当前目标类型兼容的核心纯操作，并允许其每个输入继续使用同一输入器；数值、文本、时间、几何、条件取值、结构和集合计算共享这一交互。识图、OCR、等待、输入、文件、消息、目标访问、项目函数和扩展函数必须在中央编辑区单独占一条语句；搜索到这类能力时可以一步插入独立语句并引用结果，但不得隐藏调用。若项目函数返回坐标，用户先在调用语句绑定结果，再在后续点击参数中选择该结果；可能无结果时沿用“有结果”检查。

统一输入器没有“更多操作”或第二套计算表单。只读和编辑状态复用同一尺寸与位置，点击后仅出现光标和上下文补全，不在右侧追加取消/确认，也不在下方常驻 `@ 值` 或运算提示。键入 `@`、命名空间、`.` 和 `(` 分别触发已有值、纯操作/成员和参数提示；参数标题旁使用可访问的问号按钮按需展示当前类型示例与完整语法入口。

统一输入器的发现机制属于全局 Control 契约，不是某个函数表单的局部增强。用户只需要记住四个入口：`Ctrl+Space` 查看当前字段全部可用写法，`@` 选择变量或结果本体，值后的 `.` 逐级选择字段，`文本.`、`数值.` 等命名空间查找纯值操作。零记忆目录按“直接填写 / 已有值 / 纯值操作 / 运算与判断”组织；进入操作括号后自动收束到当前参数、类型、枚举和示例。十二个固定命名空间为“文本、数值、类型、时间、结果、条件、坐标、颜色、列表、字典、JSON、文件”，按后端权威注册表顺序投影。明确搜索到但不适用于当前字段的操作保持低权重可见并原位说明适用类型，不允许点击形成无效草稿；普通输入期间不常驻完整目录，也不因每输入一个普通文字字符弹出无关能力。

候选目录的命名空间、操作名、用途、关键词、参数标签、示例、兼容状态、字面量和运算符全部来自版本化值目录。前端只负责当前光标上下文、分组、键盘焦点和插入投影，不得复制操作能力表或从稳定 ID 推测名称。注册表加载失败时保留当前草稿和直接值编辑，显示一次可恢复说明；不得回退到半套手写操作，也不能让帮助文档声称存在输入器无法创建的语法。

文本替换/拆分、类型转换、可选默认值、对象字段、列表字典、JSON 和几何等核心纯操作统一投影为 `命名空间.操作(参数…)`，选择补全后自动插入括号和参数占位。普通文字顶层无需引号，函数参数中的文本自动补齐成对引号。提交后仍保存稳定类型化值树，输入文字不成为项目事实；非法草稿保留在本地并就近解释如何修复。

局部赋值检查器必须显示变量的用户名称和中文类型。常用静态类型可以直接切换；切换类型时，目标类型和新的空/默认值在同一个结构命令中提交，不能留下“变量已经是新类型、值仍是旧类型”的中间状态。选择变量或计算结果后，局部变量类型跟随所选结果原子更新；项目变量的类型由项目定义固定，赋值页只读显示。高级列表、字典、记录和可选类型即使不在常用类型菜单中，也必须保留其完整类型并能通过引用或计算结果赋值，不能为了简化界面降低 ProgramDocument 表达能力。

“如果”的判断条件也使用同一个布尔输入器。最常见的布尔值、比较、`且 / 或 / 非` 直接填写或从 `@` 候选插入；只有用户键入某个类型命名空间时才显示类型兼容的纯值操作，不默认展开 JSON、集合或无关能力。输入器必须把比较与布尔组合提交为现有强类型条件节点，而不是保存公式文本。

返回 `bool` 或 `optional<T>` 的调用在“返回”区块下提供一个次级动作“根据结果添加条件”。点击后原子地完成结果命名和下一条条件创建：布尔结果显示“如果「名称」成立”，可选结果显示“如果「名称」有结果”。默认名称来自函数业务名，允许后续修改。中央两行使用轻量连续关系提示，但不合并成一张大卡片；这样既减少手工绑定和查找变量的步骤，也不会隐藏截图、OCR、等待等真实调用或改变断点顺序。

简单一层计算在检查器中就地展开。复杂计算进入检查器内的“计算聚焦模式”：标题同时写明所属语句和字段，顶部面包屑写明当前计算节点，中部只编辑当前节点及其直接输入，返回上层时恢复触发输入的焦点。切换为固定值或已有值时，旧计算候选面板立即关闭。界面允许逐层展开、折叠、包装、解包、复制、移动、返回上层和查找引用，不把整棵递归树一次铺满窄栏，也不要求用户阅读公式文本。节点错误在本层就近显示并向祖先聚合；从问题面板进入时自动展开必要祖先并聚焦对应 `value_id`。

固定列表与字典继续使用多行结构编辑器管理项、键值、排序和重复键，记录与 JSON 使用各自的强类型结构 Control；其中每个可自由填写的叶子复用统一输入器。简单与复杂纯计算不再打开第二套“更多操作”表单，而是在输入器内通过 `@`、类型命名空间和参数提示完成。结构编辑器保留根与未替换子节点的稳定 `value_id`，支持显式保存/取消、放弃脏草稿确认、严格 JSON、重复字典键校验和三层后折叠。

中央区为复杂参数显示一行摘要，例如“位置 · 根据窗口中心和按钮偏移计算”，不把内部每个操作变成流程语句。用户可以把选中计算子树提取为独立赋值或返回值项目函数；只有作用域、求值次数、惰性分支、可选值收窄、错误时机、来源目标和 Player 绑定都能保持时才显示该提取目标。提取是可读性工具，不是超过固定层数后的强制降级；不能安全提取时解释原因，不允许普通确认框放行语义变化。

每个可配置值节点以稳定 `value_id` 参与选择、诊断、调试和 Player 绑定；操作输入槽使用稳定 `operation_input_id`，不依赖左右顺序或显示名。Player 设计器既可暴露整个参数，也可暴露开发者选定的嵌套值；复制产生新绑定候选但不复制已有字段，重排、折叠、改显示名和无关编辑不得使既有绑定漂移。改变来源或操作若会删除非空子树，先展示影响并作为一个可撤销命令提交。

必须区分四种状态：

1. Control 内尚未提交的输入草稿。
2. 已提交但语义未配置/不合法的字段值。
3. 结构命令因 revision、权限或引用失败。
4. 项目事务/磁盘持久化失败。

字段草稿不能成为长期平行参数模型。切换语句时，合法草稿按 Control 提交规则提交；不合法草稿保留并请求用户修复或放弃，不能静默丢弃，也不能伪装成 ProgramDocument 已保存。

### 8.1 变量工作区

前台只使用“局部变量”和“项目变量”。项目变量从程序工作区进入，不增加独立活动栏；局部变量由语句选择器、成员选择器、引用导航和调试器呈现，不要求用户先在面板创建。

函数参数、条件叶和纯值输入使用统一的可搜索候选面板，而不是随着变量数量增长而失控的原生长下拉框。输入 `@`、键入限定名称或点击插入入口都可以打开候选；按“局部变量 / 项目变量 / 前序结果 / 当前作用域”分组并搜索名称和类型。选择后，输入内容转成显示来源、名称与必要类型的稳定词元。同名值依靠来源、类型和所在作用域区分；任意粘贴文字不能伪造引用，但同一项目内复制的 EasyCode 词元可以保留或重新解析稳定身份。

项目变量使用可搜索列表，不再按用户/上下文/系统分类，也不显示配置/运行/持久模式。每行只显示名称、值摘要和类型；说明、默认值、约束、引用和危险操作进入检查器。列表和字典使用结构化 Control，不默认要求填写 JSON。

完整集合 Control 区分：简单列表的逐项/多选/标签、记录列表的可排序表格或重复卡片、字典的键值表格、嵌套集合的可展开子表，以及外部 JSON 的树形查看和 Schema 转换。结构化筛选、转换、排序、分组和统计在中央显示输入、规则与结果摘要，右侧使用当前项/序号或当前键/值的条件和值编辑器；普通用户不接触 `filter/map/reduce` 或匿名函数。

循环调试显示快照项数、当前项和当前序号；集合变换显示输入/输出数量及关键依据。大型集合使用虚拟化、搜索和有界预览，完整值按需打开，不把全部内容刷入日志。Player 复用同一 Schema 和原子 Control，并由开发者决定新增、删除、重排、字段编辑和 JSON 导入权限。

单击项目变量编辑，F2 重命名，查找引用定位到项目函数语句与 Player 字段。删除前展示真实影响；有引用时默认阻止。调试器区分开发者默认值、运行方案初始值和当前运行值，运行期修改不回写前两者。

Player 暴露只在 Player 设计器中配置，按稳定 `variable_id` 或语句参数绑定，不在变量行上放常驻开关。

## 9. 资源工作区

资源工作区使用三栏布局：左侧目录树、中间网格/列表预览、右侧元数据与引用。`image`、`ocr`、`page` 三个通用资源根目录始终可见、可切换；无论从选择还是捕获保存进入，入口只决定初始默认目录，不能限制允许目录。`page` 在这里仅是资源分类，不启用任何页面能力。

资源以稳定 `asset_id` 被 ProgramDocument 或扩展数据引用，显示名与文件名可以修改。右侧展示尺寸、参考目标尺寸、来源、创建/修改时间、哈希、捕获审计元数据和引用位置。捕获区域只作为来源记录，不作为其他调用自动套用的运行区域。

选择资源采用两阶段提交：单击只选中，确认后向当前 `statement_id + parameter_id` 提交结构命令；提交失败保留选择和上下文。删除或移动经后端事务同步引用，有引用时展示影响并默认阻止悬空。

网格使用渐进渲染、缩略图缓存和滚动增量加载；只解码当前批次，不在切换或空闲时全量刷新。

### 9.1 运行记录与离线回放工作区

“运行记录”使用与资源、Player 一致的三栏框架，但内容职责固定：左侧按项目、日期、目标和状态筛选录制会话；中间显示当前帧、目标片段与虚拟化时间线；右侧显示帧元数据、用户/运行标记、比较设置、分析函数参数和版本证据。会话、帧和分析报告只读投影本机 Recorder/Replay Service，不建立前端假数据。

多实例 Player 的实例行只承担批量管理所需的高频动作：按真实状态显示运行/继续、暂停、停止，另提供刷新工作进程与删除；全部使用与项目一致、带可访问名称的紧凑图标按钮。未形成完整往返模型前不显示“独立窗口打开”。“复制摘要”和“诊断包”只在所选实例的右侧运行管理区显示为带文字动作；实例工作进程失联时，控制台用同一位置的恢复面板承接，不能依赖已经失联的实例 iframe。导出必须先确认收到非空 ZIP 才提示成功，不自动上传。普通失败优先复制摘要或导出轻量诊断包；只有开发者明确需要画面时间线、且发布包已开放录制能力时，才引导用户开启完整录制。

Player 控制台复用 IDE 的可访问面板拖拽规则。实例栏可在 220–480px 内调整；Player 运行栏可在 280–480px 内调整。运行栏、配置内容以及整个实例详情区都必须有可发现的收起/展开入口；最简状态只保留可直接控制运行的实例列表。拖拽条支持鼠标、方向键、Shift 加速与双击复位，宽度和折叠状态只属于本机视图状态，不进入项目、运行方案或发布包。窄窗口退化为连续布局，不允许通过固定最小宽度把原生窗口伪装成不可缩放。

时间线把缺帧、目标切换、方向/尺寸变化、断点、失败和用户标记显示为不同但克制的轨道标记。拖动只改变当前帧；逐帧、跳到标记、倍速浏览和范围选择使用同一播放控制组。大录制只解码视口附近帧，缩略图和比较结果按内容哈希缓存；切换会话立即取消旧解码和分析任务。

右侧默认只展示完成当前任务所需的内容：查看帧时显示来源目标、时间、尺寸和完整性；选择比较时显示两帧与差异摘要；选择分析时才显示可回放的官方图像/OCR 函数及其 Control；选择导出或删除时才展开空间、隐私与影响。输入、文件、消息、网络、控件与第三方扩展函数不出现在首版回放动作中。

录制入口与运行状态相邻，但不把一排录制参数塞进标题栏。未录制时使用“开始录制”主动作；录制中显示持续时间、空间、丢帧与“停止录制”，磁盘保护停止后明确说明任务仍在继续。Player 的录制设置进入运行方案/诊断设置，默认关闭；运行中提供“停止录制但继续任务”。Android 运行记录使用独立单列页面：会话列表 → 帧查看 → 分析/导出详情，不压缩桌面三栏。

导出先选择“画面与报告”或“可重现分析包”。前者默认排除项目逻辑、资源与分析快照；后者逐类展示将包含的 ProgramDocument/ECIR、资源、模型、参数与敏感画面，只有显式勾选后才加入对应 `AnalysisInputBundle`。摘要同时显示会话时间、目标、大小和帧完整性；两种包都生成内容与校验清单。删除录制必须显示受影响的分析报告、锁定状态和可释放空间；活动录制、进行中的导出/分析和被用户锁定的会话不能直接删除。

## 10. 扩展管理

扩展管理工作区必须真实展示来源、作用域、版本、锁定状态、权限、依赖、支持平台、启用项目、引用和契约测试。导入、启用、禁用、更新和卸载都有后端事务、失败回滚和问题定位。存在 ProgramDocument、Player、资源或其他扩展引用时，禁用/卸载默认阻止。

功能扩展只能通过批准的贡献点加入工作区、命名空间数据、编译步骤、运行模块和 Player 模块；入口、权限、后台状态、引用和发布体积必须能在同一管理页追踪。禁用后回收所有入口与后台，不留下灰色占位。

首版不显示页面管理、页面拓扑或随机弹窗配置。资源工作区中的 `page` 目录仍与 `image / ocr` 一样可选择、录入和引用，但不会注册页面函数或启动任何后台能力。

## 11. Player 表单设计器

三栏结构固定为左侧字段与区块树、中间 Player 实时预览、右侧字段属性。左侧不提供可伪造绑定的通用控件工具箱；开发者只能从后端 `binding-sources` 目录添加当前编译器能够真实发布的项目变量、函数参数、语句参数根、函数动作和具体目标设置。默认不暴露任何内容，目录不可用时不降级为手工填写 ID、类型或指纹。

绑定选择器按来源和项目函数分组，不把所有语句参数堆进一个“项目函数”长列表。每项主行只显示当前动作与关键对象；次行显示所属函数、语句上下文和稳定 ID 的短投影。搜索覆盖名称、路径、完整 ID 与短投影；粘贴一个语句 ID 后，就地反馈“已定位到此语句的 N 个可发布参数”。完整 ID 只通过语句检查器标题旁的低噪复制按钮提供，不常驻占用语句行宽度。检查器同时提供“在 Player 中查找”主路径，只跳转并精确筛选，不猜测参数、不默认创建控件。

重复视觉语句使用统一摘要契约在图片参数原位显示“紧凑缩略图 + 简短资源名”，不再把资源缩略图放在行首；悬停或键盘聚焦后的有界大图只提供补充，不承担基础理解。自动时间戳名称在中央投影为“未命名图片”。缩略图只为虚拟列表当前渲染行加载并按资源复用请求，不参与绑定正确性；加载失败或引用失效时保留同尺寸图片图标、可读名称、明确失效状态和全部操作，不显示资源 ID。`step_label` 已填写时作为最优先的人类上下文；未填写时系统自动摘要，不强迫用户给每条语句维护名称。

绑定使用 Schema 3 判别式稳定契约，明确区分函数参数、项目变量、语句参数根、函数动作和目标设置，不依赖显示名、树路径或投影位置。字段必须原样保存目录提供的 `binding + source_type + type_fingerprint + constraints + required`；函数契约声明必填时，外观配置不能将其降为可选。嵌套 `value_id`、资源槽和目标引用只有在编译器发布对应签名覆盖槽后才能进入目录；当前没有承接时不显示入口或“即将支持”占位。预览与运行 Player 使用同一 Control Registry、Schema、校验和默认值；Windows 与 Android 运行端共用同一编译后页面组件，通过响应式布局适配桌面与移动端，不复制第二套字段渲染器。

Player 只能覆盖开发者发布的签名值槽，不能改变表达式操作、固定值/已有值/计算值来源、引用目标或结构。作者侧字段以稳定 `control_id` 保存判别式源绑定，编译后对应 ECIR 覆盖槽；运行端不读取 ProgramDocument。一个参数根与其后代不得同时形成重叠字段；设计器就近标出冲突并要求选择暴露层级。运行方案加载时重新校验绑定目标、类型指纹、约束和平台能力；过期绑定阻止运行，不静默使用开发者默认值。

设计器的“预览运行”不是本地假预览：仅在表单已保存且无未提交修改时调用真实 `preview-run`，以默认目标、当前表单改动和所选函数动作进入同一 Program 编译与 Runtime；运行状态、停止和日志继续使用真实会话。独立 Player 使用开发者默认值、已保存运行方案、本次改动三层覆盖，只提交相对所选方案的本次变化；目标按“本次明确选择 → 方案目标 → 项目默认”解析。

Player 最终界面只保留项目名称/版本、运行方案、配置表单、真实状态、开始/暂停/继续/停止和开发者选择的日志。目标配置、资源替换和诊断只在开发者显式提供且平台支持时出现。

桌面 Player 有多个表单页面时使用“页面导航 + 表单 + 运行区”，只有一个页面时必须移除空导航列，使用“表单 + 290px 运行区”。表单正文最大宽度约 760px 并居中，不因大屏拉成长行。低于 760px 后成为布局容器内的连续滚动单列：先完整表单，后运行区；不得继续硬挤双栏，也不能让运行区或底部字段无法滚动到达。保存过的运行方案在重开时恢复上次有效选择，失效时回退首个保存方案。

字段属性中使用渐进披露的“允许终端用户操作”区。系统按字段类型列出可开放动作，开发者逐字段、逐平台勾选，默认全部关闭：坐标可拾取坐标，区域可框选，图片可从当前画面截取或从设备选择，颜色可从当前目标画面取色，控件选择器可捕获控件，手势路径可录制，文件/目录引用可打开对应系统选择器。这里只决定 Player 是否向终端用户提供主动操作，不代表提前取得系统权限。

Player 中单一动作紧邻字段显示低噪按钮；同一字段多个动作时保留一个主动作和“更多”菜单，不铺满一排图标。真实可用性由作者许可、字段类型、发布能力、宿主/目标能力和当前权限共同决定：结构上不支持的动作不显示，临时不可用但可恢复的动作禁用并原位说明。运行、暂停或任务排队时，方案修改与捕获均禁用并解释原因。

控件引用、手势路径、文件和目录只显示图标、状态摘要和必要的授权/类型信息，不提供原始 selector、JSON、URI 或路径文本框。坐标和区域保留可微调数值框；图片保留发布资源选择，并在作者开放时提供截取或设备选图。这样 IDE 与 Player 复用同一 Control 语义，但不会为了“可编辑”而把强类型值降级为字符串。

捕获或选择在确认前只形成本地草稿。取消、系统返回、拒绝、永久拒绝、权限撤销、目标离线、方向变化或宿主失败都保留字段旧值；确认后才原子提交。点/区域/截图/路径采集使用预览页中的“使用”作为确认；文件/目录则使用系统选择器的“打开/保存/选择”作为确认，返回 Player 后不重复询问。图片截取/选取进入当前 Player 实例的私有资源覆盖并提供恢复开发者默认；普通文件和目录保留系统授权的类型化外部引用。两类流程在网络或磁盘提交前都要二次验证字段目的地与 revision。Android 不显示手输路径，使用系统文件选择器；永久拒绝后显示“前往设置”，不能循环弹权限框。

目录字段的普通“选择文件夹”只授予作者声明的基础读写能力。若字段将进入 `目录.递归删除`，检查器在“危险操作”区单独显示函数位置、实际权限和不可回滚语义；作者确认后发布报告仍单列该能力。IDE 调试在解析出实际授权根后、Player 在解析当次运行的实际目标后，都使用同一危险确认页展示“此调用可删除该树内最终解析目录”、根目录保护和“部分失败不会恢复已删除内容”。确认仅对当次执行有效，不写入运行方案或定时计划；取消确认不修改方案，也不启动任务。目录复制/移动的冲突 Control 只提供报错、跳过、自动重命名，不显示合并或覆盖选项。

Windows IDE 中的 Player 设计器仍使用三栏，但可以切换 Windows 与 Android 预览视口。Windows 与 Android 运行 Player 由同一页面组件响应式投影：Android 采用单列小屏布局，顶部显示项目、方案和权限/目标状态，中部按区块渲染表单，底部使用安全区内固定的主要运行操作；日志和高级诊断进入独立页面或底部抽屉，不把桌面三栏压缩到手机。移动端所有直接操作目标不小于 44px，横向成组动作在窄屏改为全宽或底部动作表。字段较多时保留区块目录、错误摘要和“跳到首个问题”，系统返回优先关闭选择器/抽屉，再提示是否离开正在编辑的方案或正在运行的任务。MediaProjection、无障碍、SAF、APK 安装和全屏捕获确认仍使用 Android 原生宿主，但返回 Player 后使用同一状态与反馈组件。

Android 权限不混进普通参数表单。运行前检查只列当前发布内容实际需要的权限，并区分“未授权 / 已拒绝 / 被系统撤销 / 可前往设置恢复”；用户完成系统授权后返回原字段或运行操作。横竖屏、字体放大、显示缩放和系统安全区不能遮挡主要操作，方向变化不得重建运行任务或丢失未提交字段草稿。

发布前验证字段绑定、ECIR、目标、扩展权限、依赖闭包与资源完整性。禁用扩展时同时检查其贡献的 Player 控件和函数绑定。

### 11.1 连接状态与离线反馈

发布报告从实际调用与扩展权限显示三种结果：“可完全离线”“需要本机或局域网”“需要公网”，并列出对应函数、扩展与用途。不要让开发者维护一个与真实依赖可能不一致的“允许联网”总开关。

离线是合法状态。完全本地项目断网时不显示常驻红色横幅、错误徽标或阻塞弹窗；环境详情可以显示“本地运行就绪”。网络信息只在项目确实需要连接、相关语句开始执行、消息状态变化或用户主动打开环境详情时出现，不使用 `navigator.onLine` 代替真实调用结果。

连接失败必须说明层级和影响范围：公网不可达、局域网不可达、消息协调器不可达、收件实例未上线、远端服务错误和更新尚未检查不能共用“离线”文案。显式联网函数的错误就近进入当前语句与运行输出；消息保留每个收件副本状态；更新失败使用安静的就地提示，不能阻止 Player 打开或中断运行/暂停现场。只有已经缓存且超过宽限期的可信强制策略可以阻止旧版本新开任务，服务失败本身不能制造限制。

商业授权入口只有对应能力端到端存在时才显示。未来若启用，本地凭据临近到期可以非阻塞提醒；任务运行中到期不打断当前任务，过期只影响下一次新任务。具体有效期、宽限期和刷新动作由商业授权规格决定。

Windows 与 Android 使用相同语义。Android 飞行模式下，本地项目仍显示正常运行状态；小屏端不占用首屏放置无关联网警告。

平台不自动上传运行结果、崩溃、日志、截图或在线状态。诊断保存在本机，“导出诊断”由用户主动触发并在导出前说明内容范围。作者托管页只展示服务端真实拥有的请求量、传输量、配额和发布审计，不出现在线设备、设备地图、安装成功率或业务成功率等推测面板。

### 11.2 在线更新

更新入口不常驻占用 Player 主表单。作者端在发布工作区配置“项目内容更新”和“Player 应用更新”，默认关闭；首次开启时默认提供“EasyCode 托管”，高级设置允许选择兼容自建源。官方托管使用登录、项目、通道、基础配额与使用量的完整页面；自建源使用同一发布流程，只额外配置 HTTPS Feed/对象存储连接并完成真实连接测试。发布界面先用一句结论解释本次属于“仅更新脚本内容”还是“需要更新 Player 应用”，再列出触发原因。签名角色、清单哈希、CDN、ETag 等技术信息只进入发布详情与诊断。

Player 只在存在需要注意的状态时，于标题区或设置入口显示克制状态点。统一状态文案为：更新已关闭、尚未检查、正在检查、当前已是最新版本、有可用更新、正在下载、已下载并等待任务结束、需要重启 Player、需要安装新版 APK、检查失败且当前版本仍可使用、验证失败并已保留当前版本、新版本启动失败且已恢复上一版本。“已是最新”只能在成功取得并验证元数据后显示；断网不能伪报。

作者启用更新后，Player 设置页按“自动检查普通更新 / 自动下载 / 自动应用”显示三个独立开关，并提供常驻的“手动检查”动作。每个开关下方只在需要时显示一句结果：关闭普通检查说明“不再自动检查可选版本；安全策略检查仍会运行”；关闭自动下载说明“只提醒，不下载”；关闭自动应用说明“下载后由你确认”。偏好跨版本保存，手动检查和作者普通发布都不能改变开关。作者打包时关闭更新则整组设置、入口和状态点都不存在，不展示不可用占位项。

运行和暂停期间不弹出要求立即安装的模态框，也不能替换当前版本。下载完成后显示“任务结束后应用”；有未提交运行方案草稿时继续延后。更新改变 Player Schema 后，只对真实不兼容字段显示“需要检查”摘要并跳转对应 Control，不要求用户重新填写全部方案。

Android 项目内容更新不触发 APK 安装提示。必须升级 APK 时，先解释 Runtime、权限或原生组件变化，再由用户主动进入系统安装流程；系统要求确认、用户取消或安装失败均返回原 Player，并明确旧版本仍可使用。更新关闭时不显示徽标、不保留灰色入口，也不发生更新网络请求。

官方托管的基础配额不足时，在作者发布页显示已用量、限制、受影响动作和清理/等待/切换自建源等真实恢复方式，只阻止新的上传或发布。首版不显示 VIP、购买流量或升级套餐占位入口；终端 Player 不出现作者账号、配额或付费信息。更新源迁移必须通过受信任的签名发布流程，不能让普通 URL 输入框直接改写已安装 Player 的信任来源。

稳定发布详情使用清晰的阶段条：“白名单 → 百分比灰度 → 全量”。摘要只显示版本、当前比例、白名单数量、发布/暂停状态和最近策略时间；没有遥测时不显示安装数、成功率、崩溃率或业务成功率。动作栏只保留“暂停发布 / 扩大范围 / 全量发布”，低频的白名单、签名策略和发布诊断进入“更多”。扩大范围提供常用比例和精确输入，但只能高于当前值；继续发布恢复暂停前的同一范围，不能暗中扩大。

暂停确认必须写清：“停止向已取得暂停策略且尚未安装的终端继续提供此版本；已经安装的版本不会回退，离线终端可能暂时继续遵守旧策略。”取得暂停策略后，未应用暂存包停止应用并清理；已经激活或本地恢复上一槽的终端只记录真实状态。发布历史以时间线显示白名单、扩大、暂停、继续和全量动作，并能展开作者、旧值、新值与策略 revision。

Player 设置把匿名代码称为“测试分组码”，只提供复制和重新生成，辅助说明为“当前项目在本机随机生成；交给作者后，作者可以把此安装加入测试；不包含硬件、账号或运行数据”。终端没有“加入测试通道”开关；是否进入测试由作者白名单决定。重新生成前说明会退出原白名单并改变后续灰度分组，已安装版本不变；移出测试后不自动降级，等待稳定版本追平。普通终端无需显示自己属于多少百分比；未命中时使用“当前暂无面向此安装的更新”，不能写“全网最新”。

### 11.3 计划与实例中心

Windows Player Hub 使用两级布局：左侧为“计划 / 批次 / 实例”，中间显示当前列表或时间线，右侧编辑所选对象。新建计划先选一次、每天或固定间隔，再选择一个批次；批次由一个或多个“目标实例 + 已安装项目 + 运行方案”条目组成。每行只显示对象、最近/下次运行、真实状态和必要错误，时区、错过触发、重叠策略、错峰与远程启动权限进入检查器。

同一实例的条目始终按队列串行，不向用户暴露无效的并发开关；不同实例可以选择同时或按间隔错峰。跨电脑实例首次通过短期配对码或二维码确认，配对完成后再单独开启“允许此设备远程启动”。未知实例立即报错；已配对但离线的条目标出等待截止时间，不伪报已启动。删除配对或撤销权限前展示受影响计划。

Android Player 的计划页只列当前 APK 的运行方案，不显示其他 APK 或 Windows 项目。系统后台限制可能延迟触发时，使用“系统可能延后执行”而不是承诺精确到秒；用户强制停止应用后，恢复入口说明需要重新打开应用。计划与远程派发是 Player 控制面，不在函数目录增加 `计划.*` 或用普通消息模拟。

每次发布的“更新要求”使用可选/强制二选一，默认可选。选择强制后才展开生效时间、宽限期和原因，并在确认区明确说明：不会打断正在运行或暂停的任务；宽限结束后旧版本不能开始新任务；离线客户端一旦取得策略也会遵守。覆盖平台缺少兼容产物时，发布按钮禁用并逐个平台解释原因。

终端进入强制受限状态时，不使用全屏恐吓弹窗，也不能退出应用。运行主按钮替换为“更新后运行”并说明最低版本、原因、宽限状态和真实失败；日志、诊断导出、运行方案和设置保持可用。任务运行或暂停期间收到强制策略，只显示“将在本次任务结束后要求更新”的非阻塞提示。

宽限期内的强制更新仍尊重用户操作节奏：状态区显示截止时间、原因、预计大小以及“现在更新 / 稍后”两个主次动作，不偷偷打开自动下载或自动应用。宽限结束后移除“稍后”，但不自动安装；Android 继续进入系统确认页。终端关闭普通自动检查时明确说明“不会检查可选版本；已启用的安全策略检查仍会以低频运行”，不能把两类检查混为同一个开关。网络详情列出该维护连接；作者打包关闭全部更新时才显示“不会产生更新网络请求”。

## 12. 运行目标

- **Windows 窗口**：按标题/句柄绑定，优先后台输入；失败时仅在项目允许后回退物理输入并记录日志。
- **Windows 桌面/全屏**：没有可靠窗口归属，固定允许物理输入；执行后恢复鼠标位置，不恢复旧前台窗口。
- **Android ADB**：设备发现只提供候选列表，开发者必须明确确认并保存非空序列号；离线或序列号失效时要求重连/重绑，不自动选择唯一在线设备，也不得伪报绑定成功。
- **Android 本机**：属于首个正式版本目标，但只有真实宿主、权限、Capture、输入、文件与签名 APK 闭环完成后才显示为可用；未通过真机验证时不得用 ADB 测试状态替代。
- **无操作目标**：纯计算、文件、消息和日志可以运行，视觉与输入在编译/运行前明确阻止。

目标工作区使用三栏：左侧是“无操作目标”和已保存目标列表，中间只展示当前已保存绑定摘要、引用和空状态，右侧检查器编辑当前本地草稿。创建和修改在点击“保存目标”前不写入磁盘；带草稿切换、删除或离开时必须明确保存或放弃。保存使用独立目标 `expected_revision`，409 保留草稿并要求重新载入，不自动合并或覆盖。删除前查询真实引用，有引用时阻止并可定位。窄宽时右侧改为抽屉；列表、表单、保存和放弃路径全键盘可达。

目标行只显示名称、类型、绑定摘要和后端真实提供的状态；当前后端没有在线探测契约时，不显示“在线/已连接”假数据或测试按钮。当前默认调试目标在状态栏显示。

目标引用使用专用 Control。Windows Player 可配置 Windows 与 ADB 目标；切换由 ProgramDocument 的 `target_scope` 结构表达，不通过工具栏临时改变程序语义。一个实例任意时刻只有一个当前目标；并行运行使用 Player 多开器创建隔离实例。

Windows 同名窗口仍使用同一个目标 Control：窗口标题输入保留为规则编辑能力，右侧使用与取点/框选相同的纯图标“捕获窗口”和“测试窗口”动作。捕获在全桌面冻结帧中点击窗口，回填后主表单只显示业务标题与“已捕获当前实例”；窗口类名、程序名、PID、HWND 和捕获时间进入按需诊断。手改标题或匹配方式立即解除旧实例绑定。测试必须高亮实际解析窗口；同名歧义时原位要求重新捕获，不能弹出只有一行字段的额外配置窗口，也不能默认选择第一项。

Android 本机 Player 只显示“当前手机”，不向终端用户展示 Windows/ADB 切换。Windows IDE 的目标工作区将“ADB 设备调试”和“Android 本机测试 Player”分为两个明确动作：前者由 Windows Runtime 操作设备，后者安装/连接手机内 Runtime。状态、日志与测试报告都显示实际执行宿主，避免把两者误认为同一种测试。

### 12.1 右侧检查器任务卡与收敛路径

右侧检查器的首要用户是正在编排脚本、认识业务变量但不应理解内部 ID 的作者。其最短任务固定为“确认当前对象 → 完成本次必填决定 → 按需即时测试 → 处理结果 → 必要时展开低频设置”。程序、资源、项目变量、运行目标、Player、回放、扩展和计划都使用这一任务语法；领域数据不同，视觉层级、状态与操作位置不另起一套。

现状摩擦按严重度冻结如下：

- `[P1]` 函数、资源和引用把稳定 ID 当作主标题或正常值，作者无法先确认业务对象。
- `[P1]` 图像/OCR 的测试被拆成独立区块，资源又被通用表达式输入器包裹，完成一个参数需要理解额外中间步骤。
- `[P1]` 同排 Control 与选择、捕获动作不同高或换行，使用户误判动作是否属于当前字段。
- `[P2]` 参数、结果、高级设置、备注和技术信息在不同工作区使用不同字号、边界和间距，切换对象时层级重新学习。
- `[P2]` 有安全默认值的解释与低频技术信息常驻，挤压真正需要决定的字段。

目标路径只有四个可见层级：语义化检查器标题；“参数 / 结果 / 基本信息”等主要区块；字段标签与 32px Control/字段动作；就近反馈或 28px 低频折叠。视觉/OCR 的“测试”属于参数区块标题动作，结果在同一区块就近返回；资源直接选择或捕获，不伪装成自由文本。主标题与普通列表只显示业务名称，稳定 ID 仅进入复制、tooltip 或开发者详情。

检查器在 280–360px 内保持同一密度：标题到首字段 10px、标签到 Control 5px、字段间 12px、区块间 18px。只有开关、短状态等自身宽度稳定的紧凑 Control 与标签同排；文本、数值、枚举、引用、字段动作和结构值统一使用标签在上、Control 在下的共享布局，以保留可读输入宽度。加载、空、错误、必填错误、禁用、测试中、测试成功、测试失败、未保存和保存冲突均使用同一共享状态语言；不得通过增加一张弹窗表单解决原表单能直接完成的任务。

本轮验收追踪 `REQ-UIS-016` 与 `AC-UIS-INSPECTOR-001..005`。自动化验证共享原语、语义标题、资源直选、单行几何和状态；真实浏览器分别在 1440×900、1024×768 与 760×720 走查函数、资源、目标及可达的空/错误状态。Windows Player 与 Android 真机证据必须单独记录，不能由桌面浏览器截图替代。

## 13. 运行输出、问题与调试

三者统一进入一个可调高度的底部反馈面板，以“输出 / 当前值 / 问题”标签切换。外壳唯一持有拖拽高度、标签、通用动作和关闭按钮；内容页只能填满剩余区域，不能再声明固定面板高度。活动栏运行入口打开“输出”，状态栏问题计数打开“问题”，再次点击当前入口收起。三者只合并容器和入口，不合并数据语义。

问题面板承接 Schema、引用、类型、平台、扩展、编译、自动保存、捕获、目标和运行失败。每项显示级别、短消息、修复建议、稳定 `function_id + statement_id + value_id` 的可读字段投影，以及真实可用的“重新执行”或“再次尝试”恢复动作。点击后选择对应语句并聚焦字段；这些人工恢复动作不得与 ProgramDocument 异常区域重试策略混称为同一入口。

运行输出使用结构化事件，不用 Emoji 充当类别。默认展示全部，分类按真实事件动态出现：程序、目标与输入、视觉、网络、消息、计划、扩展、警告与错误。不存在事件的分类不显示空标签。筛选显示数量；复制只复制当前结果；清空只清理当前前端视图，不伪造后端删除。

暂停时显示当前指令、调用栈和变量快照。调试器按需展开 `value_id` 的已求值结果、未求值/被短路状态、类型、来源、错误和集合输入/输出数量；中间值默认不刷入日志，敏感值按契约脱敏。崩溃诊断显示最后稳定语句与结果未知动作，但不提供误导性的自动续接。只有真实执行边/语句状态允许轻量动画。

## 14. 对话框、菜单与反馈

- 业务成功、失败、确认、取消和可恢复警告必须留在 EasyCode 界面内：短结果使用 Toast，字段问题就近显示，非阻断状态使用窗口内 Notice，需要明确决策时使用统一 EasyCode Dialog。禁止调用 Windows `MessageBox`、浏览器 `alert / confirm / prompt` 或任意宿主默认提示框承接业务反馈。
- 操作系统界面只用于它拥有真实授权或安装边界的任务：文件/目录选择器、Android 权限与系统设置、APK 安装。系统界面返回后继续在 EasyCode 内显示结果、错误与恢复动作，不再追加第二层系统确认。
- 对话框默认宽 420px，复杂审核 520–720px；小屏 `calc(100vw - 32px)`。
- 标题区包含标题、必要说明和关闭；主体只放当前任务字段；底部右对齐取消与主动作。
- 普通弹窗可点遮罩关闭；破坏性或有未提交内容的弹窗需要确认。
- 删除、覆盖、禁用扩展和替换描述对象与影响，按钮使用明确动词。
- 右键菜单根据对象动态生成，禁用项解释原因，不堆重复入口。
- Toast 只用于短暂结果；字段错误就近；系统失败进入问题/输出。长错误堆栈不得塞进 Toast 或参数表单。

## 15. 原生捕获宿主

桌面捕获保持原生、快速、无边框。冻结图像与目标工作区位置、DPI 和裁剪一致；冻结成功只用约 180ms 的淡绿色边缘光效确认新帧，随后恢复中性细边框，不让常驻霓虹效果与选区竞争。未选择时工具栏优先放截图下方；选中点或区域后，选区外降噪且紧凑确认工具条就近停靠并避开选区与屏幕边缘。外部遮罩不接收选择。

Windows 窗口画面按真实屏幕工作区对齐显示；ADB/Android 虚拟画面没有 Windows 屏幕位置，必须在当前显示器的安全工作区内等比居中适配。缩放只影响展示，取点、框选和路径结果始终反算为原始设备帧坐标，不得把设备画面左上角误当作 Windows 桌面 `(0, 0)`，也不得因为虚拟画面跨越显示器边界而拒绝捕获。

属性检查器发起的单次 Capture Session 默认只显示清空、取消和确认；控件捕获增加唯一的“选择父级”动作，工具条顺序固定为清空、父级、取消、确认。取色使用点选准星和就近颜色预览，确认后一次回填完整 RGBA。点击画面先框选当前位置最深的语义控件，父级可连续上移直至目标根；不提供子级按钮或原始树浏览器。确认后把当前层类型化结果提交到原请求的语句参数。连续资源创建模式才允许在同一冻结帧中持续生成内容。

控件选择器在 IDE/Player 检查器中必须投影为名称、稳定身份摘要和“捕获控件”动作，不得因为底层是 Record 就展示“14 个字段”或把用户带入 selector/JSON 编辑器。provider、祖先路径和内部区域仅用于诊断与运行，不作为常规表单内容。

回填前重新验证项目、编辑会话、请求 ID、目标、`statement_id`、`parameter_id` 和 revision。捕获期间语句被删除、选择切换、扩展禁用或会话过期时，拒绝错误回填并保留可恢复结果。

最小化或无法取得完整帧时明确提示，不返回黑条/遮挡图。原生宿主通过轻量 IPC 返回结构化结果，资源保存复用统一资源工作区。

Android Capture Session 使用“悬浮准备 → 干净原帧 → 原地冻结编辑”，不把桌面三栏界面缩进手机，也不在采集前打开带标题栏和底栏的独立页面：Player 发起动作并完成必要系统授权后退到后台，只显示可拖动贴边的 EasyCode 悬浮按钮；用户切换并操作目标应用，展开按钮后选择开始采集或取消。开始采集时整个 EasyCode 悬浮层先从窗口移除，宿主只接受时间戳晚于移除时刻的新原始分辨率帧，再用同一个 WindowManager 捕获层原地显示冻结画面。进入冻结态允许一次 120–160ms 的淡绿色边缘光效，但光效、遮罩、选区、放大镜和控件都属于显示层，不能写入帧像素或最终图片。

悬浮准备阶段允许目标应用自由切换横竖屏；点击开始后，宿主按当下真实显示尺寸刷新帧缓冲，不要求用户返回 Player 或重新申请投影。冻结编辑开始后坐标空间才锁定，此时再改变方向或尺寸会明确取消候选并保留旧字段值。Windows、ADB 与 Android 本机统一复用“准备 → 干净新帧 → 冻结选择 → 原子确认”的 Capture Session 状态、选择模型和点/区域/图片/路径结果契约；平台只分别实现截图、悬浮/顶层窗口、系统权限和输入细节，不复制字段校验、提交规则或错误语义。

冻结编辑层以原目标画面为唯一主体：点使用十字准星与局部放大，区域/图片使用选区外暗化、选区内原亮度、可拖动边角和按需微调，路径直接覆盖绘制；确认、取消、清空、撤销和微调按钮悬浮在截图内部并自动避让屏幕边缘与当前选区，不另占页面上下栏。确认后按原始帧坐标原子回填，图片只在明确框选时从原始像素裁剪；取消、返回和失败保留旧值。系统权限或文件选择器返回后恢复原会话；应用被系统终止、权限撤销或目标方向/尺寸改变时拒绝旧坐标回填，并保留可理解的重试入口。控件捕获只在 Android 宿主确实提供相应语义时出现，不能用无功能按钮占位。

## 16. 响应式与窗口尺寸

EasyCode 以 1280×720 为最低完整桌面体验，推荐 1440×900 以上。

- ≥1280：活动栏、左栏、中央工作区和检查器同时显示。
- 1100–1279：侧栏收窄，说明优先省略，核心动作保留。
- 820–1099：左右侧栏默认收起，以抽屉打开；顶栏不为重复面包屑保留空间。
- <820：只保证查看与轻量修改，不承诺完整编排；活动栏和底部面板仍可访问。

使用 Grid/Flex、`minmax(0,1fr)` 和容器查询；禁止用 `padding-left`、绝对坐标或 `transition: all` 模拟布局。动画只使用 transform/opacity，120–180ms，并尊重 `prefers-reduced-motion`。

## 17. 无障碍、国际化与输入

- 图标按钮有可读名称与工具提示；装饰图标 `aria-hidden`。
- 语句行向读屏提供名称、层级、选中、折叠、诊断和运行状态。
- 重排、缩进、移出作用域均有键盘路径，并通过 live region 播报结果；拖拽不是唯一入口。
- 计算聚焦模式、语义面包屑、展开/折叠、包装/解包、移动和错误跳转均可键盘操作；返回上层恢复触发输入焦点，层级不能只靠横向缩进或颜色表达。
- 删除或移动后焦点落到确定的邻近语句/区块，不回到页面根。
- 弹窗焦点圈定，关闭后归还触发点；视觉顺序与焦点顺序一致。
- 不只依赖颜色表达状态；点击目标 IDE 至少 28×28px，Player 至少 36×36px。
- 支持中文 IME，不在 composition 期间提交；文案允许扩展 30%，省略值可查看完整内容。

## 18. 性能预算

- 应用外壳不得被结构化编辑器、扩展管理、计划服务或可选扩展阻塞。
- 普通结构命令视觉反馈一帧内完成；局部 Schema 校验 `<50ms`，跨函数引用/语义校验 `<200ms`。
- 1,000 个函数检索 `<100ms`；10,000 条结构化语句打开后 `<1s` 可交互。
- 语句与资源列表分别验证 10,000 项虚拟化和内存上限；折叠分支不实例化不可见 Control。
- 自动保存防抖是写入策略，不计作交互延迟；切换、运行和关闭前必须冲刷或明确保留未提交草稿。
- Player 不引入完整 IDE、结构化编辑器、扩展管理器或全量 UI 库，按需打包。
- 后台任务可取消并在隐藏/禁用时停止；禁止持续滤镜、无限动画、主线程全量扫描和无条件 `transition: all`。
- 表达式性能报告单独记录节点数量、技术深度、单节点编辑反馈、祖先增量类型检查、摘要生成、聚焦首开、编译时间、ECIR 体积、Runtime 峰值内存、集合取消响应和 Player 覆盖槽应用时间；未测量前不写伪精确上限。
- 性能报告记录硬件、系统、构建类型、数据集哈希、冷/热条件、样本数、p50/p95 和测量起止点；约 3 秒冷启动与空闲 CPU 接近 0 在基线未冻结前属于观察性 SLO。

## 19. 组件实现规则

- 图标统一 `lucide-vue-next`；不使用 Emoji 或字符图标。
- 颜色、字号、间距、圆角和阴影只来自令牌。
- 优先原生语义元素与项目 Control，不依赖全局注册大型 UI 库。
- 组件覆盖 default、hover、active、focus-visible、disabled、loading、error、empty。
- API 状态覆盖 loading、empty、error、success；空态提供真实下一步，不写 “No Data”。
- 同一实体在目录、语句投影、检查器、资源、Player 和调试器间使用同一稳定 ID 和显示名。
- 扩展贡献只能使用批准的工作区/Control/命令协议；不能注入绕过主题、权限或保存事务的任意页面。

## 20. 开发验收清单

每个前端变更合并前至少验证：

1. 功能入口有真实后端承接，加载、空、错误、禁用、取消和恢复完整。
2. 键盘能完成创建、选择、重排、嵌套、参数编辑和退出，焦点不丢失。
3. 切换、失焦、保存失败、运行前冲刷和外部冲突不丢 ProgramDocument 或 Control 草稿。
4. 结构化命令、中央投影和检查器始终引用同一对象；无关稳定 ID 不变化。
5. 移动、复制、撤销、重做、删除和引用阻止符合 ProgramDocument 规则。
6. Windows、ADB 与 Android 本机平台限制在编辑、编译、运行和 Player 中一致；界面明确显示实际执行宿主。
7. Player 预览与实际 Player 使用同一 Schema、Control 和校验。
8. 1280×720、1440×900、1920×1080 和窄窗口布局可用。
9. 前端类型检查、单元测试、生产构建、无障碍扫描和反模式检测通过。
10. 独立 Windows Player 与签名 Android APK 在无开发机依赖环境启动并执行，包内无 ProgramDocument 和扩展源码；Android APK 还需在断开数据线并关闭 IDE、后端后通过真机冒烟。
11. 首版不存在页面导航包、入口、函数、后台或 Player 依赖；`page` 资源目录独立可用且不会激活页面能力。
12. 没有 Emoji 功能图标、硬编码主题色、假数据、重复入口、`transition: all` 和无意义说明。
13. 深层值可被键盘编辑并按 `value_id` 从问题面板精确聚焦；复制、移动、包装、解包、提取、撤销和重做符合稳定 ID 与语义等价规则。
14. Player 嵌套字段只覆盖已签名值槽，父子重叠、结构注入、过期绑定和类型不兼容均在运行前阻止。

## 21. 当前边界

vNext 的首个正式版本目标矩阵是 Windows IDE 开发与测试 Windows、ADB 和连接的 Android 本机 Runtime，并交付 Windows/ADB Player 与可脱离电脑独立运行的签名 Android APK。内部可以先实现 Windows/ADB 垂直切片，但 Android 真机权限、截图、输入、Capture、文件、小屏 Player、兼容扩展和打包未通过前不得正式放行；AAB 不作为首版硬门。iOS 与 macOS 不在产品范围内。

项目函数不提供可编辑源码视图；公开扩展市场、任意远程代码安装、第三方目标驱动和页面导航不属于首版。通用扩展地基使用中立 Harness 扩展验证，不以未交付领域入口作为测试样板。

若新需求改变 ProgramDocument、ECIR、扩展信任、目标平台或 Player 绑定，必须先更新产品、架构、ADR 和本文，再进入代码；不得通过局部组件临时绕过。
