# 统一值输入语法参考

> 本文件由 `scripts/generate_expression_syntax_reference.py` 从权威纯值注册表生成，请勿手工维护操作清单。

## 输入器的含义

统一值输入框把草稿解析为强类型值节点，不会把公式文字原样保存。同一段文字会按当前字段需要的类型解释；例如 `空` 在可选结果中表示无结果，在普通文本中仍是“空”字。

## 不需要记忆的发现入口

- 在任意统一值输入框按 `Ctrl+Space`：查看当前字段的直接值、变量入口、纯值命名空间、参数选项和运算符。
- 输入 `@`：只列变量和前序结果本体；选中后输入 `.`：逐级查看该值的字段。
- 输入 `文本.`、`数值.` 等命名空间：查看这一类全部纯值操作。当前字段不适用的操作仍可看见，但会禁用并说明原因。
- 选择操作并进入括号：当前参数名称、类型、固定选项和可用直接值会随光标变化；不必背参数顺序。

## 可直接输入的值

| 类别 | 推荐写法 | 保存后的含义或展示 | 限制 |
| --- | --- | --- | --- |
| 普通文本 | `当前体力不足` | 原样显示 | 文本字段中的特殊词不会被强行当作运算 |
| 引号文本 | `"旧服"`、`“旧服”`、`'旧服'` | 去掉外层引号后保存为文本 | 函数参数内推荐加引号 |
| 整数 | `18`、`-3`、`+5` | 整数 | 必须在安全整数范围内 |
| 小数/百分比字段 | `18.5`、`.25`、`-0.5` | 有限小数 | `50%` 不是百分比字面量；`%` 表示取余 |
| 真 | `开启`、`是`、`真`、`true` | 统一显示为 `开启` | 英文不区分大小写 |
| 假 | `关闭`、`否`、`假`、`false` | 统一显示为 `关闭` | 英文不区分大小写 |
| 可选值为空 | `无结果`、`空`、`null` | 统一显示为 `无结果` | 只在当前字段是可选值时成立 |
| 持续时间 | `500 毫秒`、`2 秒`、`1 分钟` | 按可读单位显示 | 也接受 `ms`、`s`、`min`；不得小于 0 |
| 动态持续时间 | `[局部 · 秒数] 秒` | 变量词元加单位 | 单位前可以是数值表达式 |
| 日期 | `2026-09-06` | `YYYY-MM-DD` | 必须是有效日期 |
| 日期时间 | `2026-09-06 18:30`、`2026-09-06 18:30:45` | 内部规范为 ISO 日期时间 | 可省略秒 |
| 时间 | `18:30`、`18:30:45` | `HH:mm` 或 `HH:mm:ss` | 24 小时制 |
| 坐标 | `坐标(480, 320)` | 坐标摘要 | 两项均须为有限数值 |
| 区域 | `区域(100, 80, 640, 360)` | 区域摘要 | 顺序为 X、Y、宽、高 |
| 列表字面量 | `["data", "roomCode"]` | 强类型列表 | 只在已知列表项类型的位置使用；字典不能直接手写 `{...}` |
| 枚举值 | `包含`、`"以后者为准"` | 当前字段的枚举值 | 可用值以表单或补全为准 |

## 变量、结果和字段

| 操作 | 输入方法 | 选择后的稳定展示 |
| --- | --- | --- |
| 选择变量或语句结果 | 输入 `@`，再从候选中选择 | `[局部 · 当前体力]` 或 `[项目 · 最低体力]`；界面用半方括号词元绘制 |
| 读取直接字段 | 选择结构化值后输入 `.` | `[局部 · 图像结果].相似度` |
| 读取嵌套字段 | 选择一层字段后继续输入 `.` | `[局部 · 识字结果].来源画面.宽度` |
| 在文字中嵌入标量 | 在普通文字中输入 `@` 并选择 | `当前体力为 [项目 · 当前体力]`；数值、布尔、日期、时间等会转成稳定文本 |

不建议手写变量词元。解析器为编辑便利仍接受 `@名称`、`局部.名称`、`项目.名称`、`[局部变量 · 名称]` 等别名；存在同名值时必须从 `@` 候选中选择。项目最终保存稳定变量 ID 和字段 ID，而不是显示文字。

可选结果（例如“查找图像”）未经过“有结果”判断时仍显示字段目录，但不允许提交字段。先使用 `结果.有结果([局部 · 图像结果])` 判断，再在成立分支读取字段；需要继续计算时也可以使用 `结果.无结果时使用默认值(...)`。

## 运算符与结构语法

| 类别 | 可输入写法 | 规范展示 |
| --- | --- | --- |
| 四则与取余 | `+`、`-`、`*`、`/`、`%` | 保留符号，并用括号表达运算层级 |
| 中文比较 | `等于`、`不等于`、`大于`、`大于等于`、`小于`、`小于等于` | 中文语义词高亮 |
| 比较符号别名 | `==`、`!=`、`>`、`>=`、`<`、`<=` | 统一显示为对应中文比较词 |
| 并且 | `条件A 且 条件B`、`A && B` | 统一显示为 `且` |
| 或者 | `条件A 或 条件B`、`A || B` | 统一显示为 `或` |
| 取反 | `非 条件`、`!条件` | 统一显示为 `非 (条件)` |
| 分组 | `( ... )` | 明确运算和条件层级 |
| 纯值操作 | `命名空间.操作(参数1, 参数2)` | 操作名和参数按语义分词显示；操作可以嵌套 |
| 逐项表达式 | `每项 => 表达式`、`逐项 → 表达式` | 统一显示为 `每项 => ...` |

列表逐项操作中可用 `@当前项`、`@当前序号`；字典逐项操作中可用 `@当前键`、`@当前值`。`且` 和 `或` 建议两侧留空格，避免与普通文字混淆。大于/小于只适用于数值、持续时间、日期和时间；等于/不等于还可以比较其他相同或兼容类型。

## 中文标点兼容

引号外的 `。`、`．` 会按 `.` 处理，`，` 按 `,` 处理，`（）` 按 `()` 处理，并接受中文引号 `“”`、`‘’`。引号内的标点保持文本原意。

## 保存后会特殊显示的内容

| 内容 | 展示规则 |
| --- | --- |
| 局部/项目变量 | 半方括号词元；两种作用域使用不同的克制文字色 |
| 字段访问 | `.字段名` 作为操作词元 |
| 运算、比较、逻辑词和纯值操作名 | 使用操作强调色 |
| 括号与逗号 | 降低为辅助层级 |
| `空/null/无结果` | 在可选字段中统一为 `无结果` |
| 布尔别名 | 统一为 `开启` 或 `关闭` |
| 图片、目标、文件、控件选择器等引用 | 显示“类型 + 业务名称”摘要；由选择器或捕获器生成，不要手写内部 ID |
| 列表、字典、JSON 和其他结构值 | 简单列表显示内容；复杂值显示类型/业务摘要，并用 `.` 逐层读取 |
| 未填写的可选字段 | 显示真实默认值或 placeholder，不伪装成已保存公式 |

## 不能写进输入框的内容

截图、OCR、等待、点击、文件读写、网络请求、消息收发、取得当前时间和随机数等会访问外部状态或每次可能返回不同结果的能力，必须作为独立流程语句插入。输入框只接受无等待、无外部副作用并可类型检查的纯值计算。

## 文本

| 写法 | 参数 | 用途 | 示例 |
| --- | --- | --- | --- |
| `文本.匹配(…)` | 实际文本，目标文本，匹配方式 | 按精确、包含或正则方式匹配两段文本 | `文本.匹配("实际文本", "目标文本", 包含)` |
| `文本.去除首尾空白(…)` | 文本 | 去除文本首尾的空白字符 | `文本.去除首尾空白("文本")` |
| `文本.取得正则分组(…)` | 文本，格式或正则 | 取得首次匹配中的全部捕获分组 | `文本.取得正则分组("文本", "格式或正则")` |
| `文本.截取(…)` | 文本，开始位置，结束位置 | 按开始和结束位置截取文本 | `文本.截取("文本", 1, 1)` |
| `文本.拆分(…)` | 文本，分隔符 | 按非空分隔符拆分为文本列表 | `文本.拆分("文本", "分隔符")` |
| `文本.拼接(…)` | 左侧，右侧 | 把两段文本连接起来 | `文本.拼接("左侧", "右侧")` |
| `文本.按正则提取(…)` | 文本，格式或正则，分组序号 | 取得首次匹配的指定分组；未匹配返回无结果 | `文本.按正则提取("文本", "格式或正则", 1)` |
| `文本.提取第一个整数(…)` | 文本 | 从混合文本中取得首个整数；没有或超出范围时返回无结果 | `文本.提取第一个整数("文本")` |
| `文本.替换(…)` | 文本，查找内容，替换为 | 替换文本中的指定内容 | `文本.替换("文本", "查找内容", "替换为")` |
| `文本.转为大写(…)` | 文本 | 把文本中的字母转为大写 | `文本.转为大写("文本")` |
| `文本.转为小写(…)` | 文本 | 把文本中的字母转为小写 | `文本.转为小写("文本")` |
| `文本.连接列表(…)` | 列表，分隔符 | 用分隔符连接文本列表 | `文本.连接列表(@列表, "分隔符")` |
| `文本.长度(…)` | 文本 | 取得文本包含的字符数量 | `文本.长度("文本")` |

## 数值

| 写法 | 参数 | 用途 | 示例 |
| --- | --- | --- | --- |
| `数值.取余数(…)` | 左侧，右侧 | 取得整数或小数相除后的余数 | `数值.取余数(1, 1)` |
| `数值.取较大值(…)` | 左侧，右侧 | 返回两个数中较大的一个 | `数值.取较大值(1, 1)` |
| `数值.取较小值(…)` | 左侧，右侧 | 返回两个数中较小的一个 | `数值.取较小值(1, 1)` |
| `数值.四舍五入(…)` | 值，小数位数 | 按指定小数位数四舍五入 | `数值.四舍五入(1, 1)` |
| `数值.相乘(…)` | 左侧，右侧 | 把两个数相乘 | `数值.相乘(1, 1)` |
| `数值.相减(…)` | 左侧，右侧 | 用左侧数减去右侧数 | `数值.相减(1, 1)` |
| `数值.相加(…)` | 左侧，右侧 | 把两个数相加 | `数值.相加(1, 1)` |
| `数值.相除(…)` | 左侧，右侧 | 用左侧数除以右侧数 | `数值.相除(1.5, 1.5)` |
| `数值.转为小数(…)` | 值 | 把数值转换成小数 | `数值.转为小数(1)` |
| `数值.转为整数(…)` | 值 | 把数值转换成整数 | `数值.转为整数(1.5)` |
| `数值.限制范围(…)` | 值，最小值，最大值 | 把数值限制在最小值和最大值之间 | `数值.限制范围(1, 1, 1)` |

## 类型

| 写法 | 参数 | 用途 | 示例 |
| --- | --- | --- | --- |
| `类型.文本转小数(…)` | 文本 | 严格解析有限小数；无法解析时返回无结果 | `类型.文本转小数("文本")` |
| `类型.文本转整数(…)` | 文本 | 严格解析整数；无法解析时返回无结果 | `类型.文本转整数("文本")` |
| `类型.转为文本(…)` | 值 | 把基础值转换成稳定、跨平台一致的文本 | `类型.转为文本(开启)` |

## 时间

| 写法 | 参数 | 用途 | 示例 |
| --- | --- | --- | --- |
| `时间.减少持续时间(…)` | 日期与时间，持续时间 | 从日期与时间中减去持续时间 | `时间.减少持续时间(2026-09-06 18:30:00, 1 秒)` |
| `时间.分钟数转持续时间(…)` | 数值 | 把数值解释为分钟 | `时间.分钟数转持续时间(1)` |
| `时间.增加天数(…)` | 日期，天数 | 在日期上增加或减少天数 | `时间.增加天数(2026-09-06, 1)` |
| `时间.增加持续时间(…)` | 日期与时间，持续时间 | 在日期与时间上增加持续时间 | `时间.增加持续时间(2026-09-06 18:30:00, 1 秒)` |
| `时间.时间增加时长(…)` | 时间，持续时间 | 在一天内循环增加或减少持续时间 | `时间.时间增加时长(18:30:00, 1 秒)` |
| `时间.格式化日期与时间(…)` | 日期与时间，格式或正则 | 按跨平台格式输出文本 | `时间.格式化日期与时间(2026-09-06 18:30:00, "格式或正则")` |
| `时间.毫秒数转持续时间(…)` | 数值 | 把数值解释为毫秒 | `时间.毫秒数转持续时间(1)` |
| `时间.秒数转持续时间(…)` | 数值 | 把数值解释为秒 | `时间.秒数转持续时间(1)` |
| `时间.解析日期与时间(…)` | 文本，格式或正则 | 按跨平台格式解析文本；失败返回无结果 | `时间.解析日期与时间("文本", "格式或正则")` |
| `时间.计算时间差(…)` | 较晚时间，较早时间 | 计算较晚时间减较早时间的毫秒数 | `时间.计算时间差(2026-09-06 18:30:00, 2026-09-06 18:30:00)` |
| `时间.计算检查次数(…)` | 超时时间，检查间隔 | 根据超时时间与检查间隔计算标准函数的有限检查次数 | `时间.计算检查次数(1 秒, 1 秒)` |
| `时间.计算相差天数(…)` | 较晚时间，较早时间 | 计算较晚日期减较早日期的天数 | `时间.计算相差天数(2026-09-06, 2026-09-06)` |

## 结果

| 写法 | 参数 | 用途 | 示例 |
| --- | --- | --- | --- |
| `结果.无结果时使用默认值(…)` | 值，默认值 | 有结果时使用原值，否则使用默认值 | `结果.无结果时使用默认值(无结果, "默认值")` |
| `结果.有结果(…)` | 值 | 判断可选值是否包含结果 | `结果.有结果(无结果)` |

## 条件

| 写法 | 参数 | 用途 | 示例 |
| --- | --- | --- | --- |
| `条件.按条件选择(…)` | 条件，条件成立时，条件不成立时 | 只计算并返回条件对应的一个值 | `条件.按条件选择(开启, "条件成立时", "条件不成立时")` |

## 坐标

| 写法 | 参数 | 用途 | 示例 |
| --- | --- | --- | --- |
| `坐标.偏移(…)` | 坐标，偏移量 | 在原坐标基础上增加偏移量 | `坐标.偏移(坐标(100, 200), 坐标(100, 200))` |
| `坐标.区域中心(…)` | 区域 | 取得区域的中心坐标 | `坐标.区域中心(区域(0, 0, 640, 360))` |
| `坐标.按中心生成区域(…)` | center，width，height | 以一个坐标为中心生成指定宽高的区域 | `坐标.按中心生成区域(坐标(100, 200), 1.5, 1.5)` |
| `坐标.缩放(…)` | 坐标，横向比例，纵向比例 | 分别按横向和纵向比例缩放坐标 | `坐标.缩放(坐标(100, 200), 1.5, 1.5)` |
| `坐标.缩放区域(…)` | 区域，横向比例，纵向比例 | 分别按横向和纵向比例缩放区域及其位置 | `坐标.缩放区域(区域(0, 0, 640, 360), 1.5, 1.5)` |

## 颜色

| 写法 | 参数 | 用途 | 示例 |
| --- | --- | --- | --- |
| `颜色.颜色接近(…)` | 实际颜色，目标颜色，容差 | 判断两个颜色的各通道差值是否都在容差内 | `颜色.颜色接近(@颜色, @颜色, 1)` |

## 列表

| 写法 | 参数 | 用途 | 示例 |
| --- | --- | --- | --- |
| `列表.任意一项满足(…)` | 列表，保留条件 | 遇到第一项满足条件时立即返回 | `列表.任意一项满足(@列表, 每项 => 开启)` |
| `列表.全部项满足(…)` | 列表，保留条件 | 遇到第一项不满足条件时立即返回 | `列表.全部项满足(@列表, 每项 => 开启)` |
| `列表.删除(…)` | 列表，序号 | 返回删除指定序号项后的列表 | `列表.删除(@列表, 1)` |
| `列表.前置(…)` | 列表，值 | 返回在开头加入新项后的列表 | `列表.前置(@列表, "值")` |
| `列表.包含(…)` | 列表，值 | 判断列表是否包含指定值 | `列表.包含(@列表, 1.5)` |
| `列表.去重(…)` | 列表 | 返回保留首次出现项的去重列表 | `列表.去重(@列表)` |
| `列表.反转(…)` | 列表 | 返回顺序反转后的列表 | `列表.反转(@列表)` |
| `列表.合并(…)` | 列表，另一个值 | 按顺序合并两个同类型列表 | `列表.合并(@列表, @列表)` |
| `列表.展平一层(…)` | 列表 | 把列表中的子列表按顺序合并 | `列表.展平一层(@列表)` |
| `列表.平均值(…)` | 列表 | 计算数值列表平均值；空列表返回无结果 | `列表.平均值(@列表)` |
| `列表.截取(…)` | 列表，开始位置，结束位置 | 返回指定序号范围内的列表 | `列表.截取(@列表, 1, 1)` |
| `列表.按位置组合(…)` | 列表，另一个值 | 把两个列表相同位置的项组合；以较短列表为准 | `列表.按位置组合(@列表, @列表)` |
| `列表.按依据分组(…)` | 列表，分组或排序依据 | 把依据相同的项归入同一组 | `列表.按依据分组(@列表, 每项 => 开启)` |
| `列表.按依据建立字典(…)` | 列表，分组或排序依据 | 用每项的唯一依据建立字典；重复键会明确报错 | `列表.按依据建立字典(@列表, 每项 => 开启)` |
| `列表.按依据排序(…)` | 列表，分组或排序依据，从大到小 | 按每项计算出的依据稳定排序 | `列表.按依据排序(@列表, 每项 => 开启, 开启)` |
| `列表.按序号取(…)` | 列表，序号 | 取得指定序号的项；越界返回无结果 | `列表.按序号取(@列表, 1)` |
| `列表.插入(…)` | 列表，序号，值 | 返回在指定序号插入新项后的列表 | `列表.插入(@列表, 1, "值")` |
| `列表.数量(…)` | 列表 | 取得列表项数量 | `列表.数量(@列表)` |
| `列表.是否为空(…)` | 列表 | 判断列表是否没有任何项 | `列表.是否为空(@列表)` |
| `列表.替换(…)` | 列表，序号，值 | 返回替换指定序号项后的列表 | `列表.替换(@列表, 1, "值")` |
| `列表.最后一项(…)` | 列表 | 取得最后一项；空列表返回无结果 | `列表.最后一项(@列表)` |
| `列表.最大值(…)` | 列表 | 取得最大项；空列表返回无结果 | `列表.最大值(@列表)` |
| `列表.最小值(…)` | 列表 | 取得最小项；空列表返回无结果 | `列表.最小值(@列表)` |
| `列表.查找首个满足项(…)` | 列表，保留条件 | 按顺序返回首个满足条件的项 | `列表.查找首个满足项(@列表, 每项 => 开启)` |
| `列表.求和(…)` | 列表 | 计算数值列表的总和 | `列表.求和(@列表)` |
| `列表.清空(…)` | 列表 | 返回同类型的空列表 | `列表.清空(@列表)` |
| `列表.第一项(…)` | 列表 | 取得第一项；空列表返回无结果 | `列表.第一项(@列表)` |
| `列表.筛选(…)` | 列表，保留条件 | 逐项判断，只保留满足条件的项 | `列表.筛选(@列表, 每项 => 开启)` |
| `列表.统计满足项(…)` | 列表，保留条件 | 统计满足条件的列表项数量 | `列表.统计满足项(@列表, 每项 => 开启)` |
| `列表.转换(…)` | 列表，每项计算 | 用纯值计算把每一项转换为新值 | `列表.转换(@列表, 每项 => 开启)` |
| `列表.追加(…)` | 列表，值 | 返回在末尾加入新项后的列表 | `列表.追加(@列表, "值")` |

## 字典

| 写法 | 参数 | 用途 | 示例 |
| --- | --- | --- | --- |
| `字典.删除(…)` | 字典，键 | 返回删除指定键后的字典 | `字典.删除(@字典, "键")` |
| `字典.包含键(…)` | 字典，键 | 判断字典中是否存在指定键 | `字典.包含键(@字典, "键")` |
| `字典.合并(…)` | 字典，另一个值，冲突处理 | 按冲突策略合并两个同类型字典 | `字典.合并(@字典, @字典, 作为错误)` |
| `字典.所有值(…)` | 字典 | 取得字典中所有值的列表 | `字典.所有值(@字典)` |
| `字典.所有键(…)` | 字典 | 取得字典中所有键的列表 | `字典.所有键(@字典)` |
| `字典.所有项(…)` | 字典 | 取得字典键值项的列表 | `字典.所有项(@字典)` |
| `字典.按依据分组(…)` | 字典，分组或排序依据 | 把键值项按计算依据分组 | `字典.按依据分组(@字典, 每项 => 开启)` |
| `字典.清空(…)` | 字典 | 返回同类型的空字典 | `字典.清空(@字典)` |
| `字典.筛选(…)` | 字典，保留条件 | 逐项判断，只保留满足条件的键值项 | `字典.筛选(@字典, 每项 => 开启)` |
| `字典.设置值(…)` | 字典，键，值 | 返回设置指定键值后的字典 | `字典.设置值(@字典, "键", "值")` |
| `字典.读取值(…)` | 字典，键 | 按键读取值；键不存在时返回无结果 | `字典.读取值(@字典, "键")` |
| `字典.转换值(…)` | 字典，每项计算 | 保留键，并用纯值计算转换每个值 | `字典.转换值(@字典, 每项 => 开启)` |

## JSON

| 写法 | 参数 | 用途 | 示例 |
| --- | --- | --- | --- |
| `JSON.删除路径(…)` | JSON 数据，路径 | 返回删除指定路径后的 JSON | `JSON.删除路径(@JSON数据, @列表)` |
| `JSON.按结构规则验证(…)` | JSON 数据，数据结构规则 | 验证 JSON 并转换为目标强类型值；失败会指出具体位置 | `JSON.按结构规则验证(@JSON数据, @JSON数据)` |
| `JSON.解析文本(…)` | 文本 | 把文本解析为 JSON；格式无效时返回无结果 | `JSON.解析文本("文本")` |
| `JSON.设置路径(…)` | JSON 数据，路径，值 | 返回设置指定路径后的 JSON | `JSON.设置路径(@JSON数据, @列表, @JSON数据)` |
| `JSON.读取路径(…)` | JSON 数据，路径 | 读取指定路径；不存在时返回无结果 | `JSON.读取路径(@JSON数据, @列表)` |
| `JSON.路径存在(…)` | JSON 数据，路径 | 判断 JSON 中是否存在指定路径 | `JSON.路径存在(@JSON数据, @列表)` |

## 文件

| 写法 | 参数 | 用途 | 示例 |
| --- | --- | --- | --- |
| `文件.目录中的文件(…)` | 授权目录，相对位置 | 在已授权目录内生成能力不扩大的文件引用 | `文件.目录中的文件(@授权目录, @相对位置)` |

## 组合示例

```text
[局部 · 当前体力] 大于等于 [项目 · 最低体力] 且 非 [局部 · 正在战斗]
当前体力为 [局部 · 当前体力]
文本.替换([项目 · 公告内容], "旧服", "新服")
数值.限制范围([局部 · 识别数值] + 5, 0, 100)
列表.筛选([项目 · 副本等级], 每项 => @当前项 大于等于 15)
字典.筛选([项目 · 角色配置], 每项 => @当前值 大于等于 3)
JSON.读取路径([局部 · 接口结果], ["data", "roomCode"])
```

输入中的显示名称只用于编辑与补全；项目保存的是稳定操作 ID、输入槽 ID、变量 ID 和字段 ID。
